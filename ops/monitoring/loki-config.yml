auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: warn

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

storage_config:
  tsdb_shipper:
    active_index_directory: /loki/tsdb-index
    cache_location: /loki/tsdb-cache
    cache_ttl: 24h
  filesystem:
    directory: /loki/chunks

limits_config:
  # Retention toàn cục 30 ngày
  retention_period: 720h
  # Giới hạn ingestion để tránh spike
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  max_query_series: 5000
  max_query_length: 721h
  # Reject log stream quá lớn
  max_streams_per_user: 10000
  max_line_size: 256KB

compactor:
  working_directory: /loki/compactor
  compaction_interval: 10m
  retention_enabled: true
  retention_delete_delay: 2h
  retention_delete_worker_count: 150
  delete_request_store: filesystem

query_range:
  results_cache:
    cache:
      embedded_cache:
        enabled: true
        max_size_mb: 100

ruler:
  alertmanager_url: http://alertmanager:9093
  enable_api: true
  enable_alertmanager_v2: true
  ring:
    kvstore:
      store: inmemory
  # Thu muc scratch de ruler bien dich rule — KHAC voi thu muc chua rule goc
  # (storage.local.directory) de tranh dung chung voi common.path_prefix (/loki).
  rule_path: /loki/rules-temp
  storage:
    type: local
    local:
      # Mount tu ./loki-rules (xem docker-compose.yml) — cau truc bat buoc:
      # loki-rules/<tenant>/<namespace>.yaml, tenant "fake" la mac dinh khi
      # auth_enabled: false.
      directory: /loki/rules-config

analytics:
  reporting_enabled: false
