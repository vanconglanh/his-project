# Tổng kết phiên điều phối — Pro-Diab HIS (2026-08-29)

> Leader tự điều phối team (backend/frontend + agent) xử lý các mục còn tồn trong `docs/TASKLIST-20260829.md`.
> Nhánh: `develop` (đã push toàn bộ). Mọi thay đổi build + test + verify thật trước khi commit.

---

## ✅ Đã xong (verify thật)

### Bug bảng B
- **B-3 (BUG-11)** hàng chờ Điều dưỡng mất BN "Đang khám": root cause = lọc cứng `status:"WAITING"`. Fix `EncounterHandlers` hỗ trợ lọc CSV nhiều status; `/nurse` truyền `"WAITING,IN_PROGRESS"`. → `EncounterHandlers.cs`, `NursePageClient.tsx`.
- **B-4 (UX#2)** toast chốt đợt CLS: chỉ báo "chuyển thu ngân" khi `total_amount>0 && UNPAID`. → `use-cls-rounds.ts`.
- **B-5 (a11y)** role listbox/option/combobox + aria cho `DrugAutocomplete`/`ReceptionCheckInForm`; `htmlFor`/`id` cho toàn bộ input `VitalSignsForm`.
- **B-6 (UX#7)** bcrypt chậm: `PasswordHasher` đọc `Security:BCryptWorkFactor` (dev=10); mig `9147` re-hash 6 tài khoản test về factor 10.

### E/Đợt 0 — vá rò rỉ dữ liệu chéo chi nhánh (bảo mật)
- Đánh số lại 3 migration trùng số với họ branch (giữ 9080-9082 branch liền mạch): `create_cls_order_rounds`→9143, `seed_cls_round_permissions`→9144, `diagnosis_primary_g06`→9145.
- Thêm branch filter: Dispensing, DTQG (submissions/status/submit), BHYT export (queries + command gate).
- **Phát hiện lỗ hổng lớn hơn**: Report Builder engine hoàn toàn mù chi nhánh — `SafeQueryBuilder` chưa đọc `BranchId`/`IgnoreBranchFilter`. Đã vá: `SafeQueryBuilder` + `Dataset.BranchAlias` + khai báo 6 dataset + `PreviewReportDefinitionHandler`. Thêm branch filter cho ~30+ report descriptor tĩnh trong `ReportRegistry.cs`.
- +8 unit test regression (SafeQueryBuilder branch + quét source handler).

### E/Đợt 1 — đa chi nhánh (FE)
- **BranchSwitcher** trên topbar: đổi chi nhánh qua header `X-Branch-Id` (không cần login lại — Q10), Zustand persist, `queryClient.clear()` khi đổi.
- Màn `admin/branches` CRUD + đặt mặc định + bật/tắt trạng thái + nav item.
- Cột **"Chi nhánh"** trong danh sách lượt khám: `EncounterResponse.branch_id`, FE map id→tên từ cache branches (tránh N+1).
- **Fix crash** phát hiện khi verify browser: `BranchSwitcher` dùng `DropdownMenuLabel` ngoài group → "MenuGroupContext missing" làm sập app. Bọc trong `DropdownMenuGroup`.

### F/Đợt 2 — thu hồi quyền (SoD)
- Mig `9146`: gỡ `cls_round.pay/waive` khỏi `bac_si`, `stock.adjust`+`report.build` khỏi `duoc_si`. Verify LIVE DB đúng.

### F/Đợt 3 — compliance (code)
- **P0-01 (deadline pháp lý 31/12/2026)**: `AuditAction.View` + ghi audit khi ĐỌC chi tiết 5 loại hồ sơ (patient/encounter/lab-rad PDF/prescription).
- **P1-03**: chặn self-verify CLS (`PerformedBy==user` → `VERIFY_SELF_FORBIDDEN`), tắt AutoVerify khi import.
- **P1-04**: report engine mask PII mặc định; chỉ lộ plaintext khi `descriptor.AllowPiiPlaintext` + quyền `report.pii_plaintext` (super admin bypass); Report Builder luôn mask trừ khi có quyền. Mig `9154`.
- **P2-06**: mig `9153` quyền `patient.clinical.write` (bac_si+ktv); `PatientsController` allergies đổi sang quyền lâm sàng → lễ tân mất quyền sửa dị ứng.
- **P2-07**: `GET /doctors/lookup` (bảo vệ `appointment.read`).

### C — nợ schema lab/rad orders (bước 1, reversible)
- Mig `9148` **non-destructive, đảo ngược được**: backup 6 bảng + copy order legacy vào họ `cli_` (giữ id).
- **Verify LIVE DB**: trước 60 dòng lab_results trỏ bảng chết / 3 trỏ cli → sau migration **0 dòng không resolve được về `cli_lab_orders`** (cli 7→17). Idempotent pass 2 sạch, không nhân đôi.

### D — design debt
- **D-1** layout `ClsRoundCreateDialog` (grid 1.2fr/1fr, line-clamp-2).
- **D-2** 13+ vị trí `text-[9/10/11px]` → `text-xs` (grep xác nhận 0 còn lại).
- **D-3** audit token màu: `docs/design/audit-token-mau-20260829.md` (thực tế 77 file, phân loại + ưu tiên + khuyến nghị).

### E/Đợt 2, 3 — schema backbone (migration + verify LIVE)
- Mig `9150` (branch_groups REGION + cột group_id/status branches + quyền `branch.group_view`), `9151` (stock_transfers state machine 8 trạng thái + items + quyền + ngưỡng 5tr), `9152` (service_branch_price_override 3 tầng + snapshot giá `bil_billing_items`). Tất cả apply + idempotent LIVE OK.

---

## 🔍 Đã verify thế nào
- `dotnet build ProDiabHis.slnx` → **0 error** (nhiều lần, gồm tổ hợp mọi thay đổi).
- `dotnet test` → **745 unit + 6 architecture + 5 integration = 756 PASS** (gồm 11 test mới).
- `npx tsc --noEmit` (frontend) → **exit 0** sau mỗi nhóm sửa.
- **Migration apply LIVE trên DB thật** (docker `prodiab-mysql`, db `prodiab_his`): 9146/9147/9148/9150/9151/9152/9153/9154 apply sạch; chạy pass 2 xác nhận idempotent; kiểm chứng số liệu thật (lab_results resolve 100%, RBAC revoke đúng, quyền mới seed đúng).
- **Browser thật** (Next dev + backend container): đăng nhập admin OK, branch switcher render + mở dropdown OK (sau khi tự phát hiện & fix crash MenuGroupContext).

---

## ⚠️ Giả định tự đưa ra — CẦN BO REVIEW
1. **F/Đợt 2 (mig 9146)**: bác sĩ KHÔNG thu tiền/miễn phí CLS tại chỗ (kế toán làm); phòng khám có người kiểm kê kho tách khỏi dược sĩ. → Nếu sai: rollback có sẵn cuối file `9146`. Nếu chỉ 1 dược sĩ kiêm kiểm kê: re-grant `stock.adjust` + bắt buộc `reason`+audit.
2. **7 câu hỏi BO (Q1,2,5,6,7,9,10)**: dùng default trong BRD, ghi rõ ở mục 15 của `docs/prd/dinh-nghia-nghiep-vu-da-chi-nhanh-20260829.md` để BO đối chiếu.
3. **P1-04**: mọi report mặc định mask PII; chỉ super admin (hoặc role được cấp `report.pii_plaintext`) + descriptor opt-in mới thấy plaintext. Nếu nghiệp vụ cần role khác xem plaintext: grant tay + set `AllowPiiPlaintext=true`.
4. **P2-06**: chỉ nhóm dị ứng (allergies) coi là "dữ liệu lâm sàng" trong PatientsController hiện tại (không có endpoint tiền sử riêng).
5. **`doctors/lookup`.specialty = null** (User chưa có cột chuyên khoa).

---

## ❌ Chưa làm / còn tồn (kèm lý do)
- **C bước 2** (rewrite ~15 JOIN report/exporter `diab_his_lab_orders`→`cli_lab_orders` + DROP bảng legacy): CỐ Ý chưa làm blind. Lý do an toàn: schema thực tế `diab_his_lab_results` đã bị ALTER khác bản CREATE 9004, collation 2 họ khác nhau; cutover phải chạy trên DB thật có kiểm 62 dòng. Bước 1 (9148) đã đưa `cli_` thành superset an toàn nên KHÔNG có regression khi hoãn bước 2. Checklist bước 2 ghi trong file `9148`.
- **E/Đợt 2 handler**: guard tìm kiếm BN theo phạm vi (BR-25) + logic group scope chưa code (mới có schema + quyền).
- **E/Đợt 3 handler**: state machine điều chuyển kho + resolve giá override 3 tầng + kiểm `PRICE_OVERLAP` chưa code (mới có schema).
- **E/Đợt 4, 5**: dashboard chuỗi/drill-down, công nợ nội bộ, BHYT theo chi nhánh, clone chi nhánh + checklist go-live — chưa làm. Đây là khối tính năng lớn (nhiều tuần), vượt phạm vi 1 phiên; report engine đã sẵn branch filter làm nền cho Đợt 4.
- **P2-08** (thu hẹp `IgnoreBranchFilter` chỉ cho dataset tài chính — ScopeMode Single/Group/All): hoãn có chủ đích, cần architect thiết kế lại, không sửa tùy tiện trong đợt vá lỗi.
- **B5 của 9141** (DELETE `user.read` khỏi lễ tân/bác sĩ): chờ FE chuyển hẳn sang `doctors/lookup` mới chạy, tránh làm hỏng chọn bác sĩ.
- **D-3 refactor** (chuyển 77 file sang token màu): chủ trương tách đợt riêng, phiên này chỉ giao audit.

## 👉 Lưu ý môi trường
- Có vài file **untracked KHÔNG thuộc phiên này**: `backend/src/**/Notifications/*`, `db/migrations/9160_notification_channels.sql` — là WIP của phiên khác, leader KHÔNG đụng tới, không commit.

---

## 📁 File quan trọng đã thay đổi (chính)
- Migration mới: `db/migrations/9146`, `9147`, `9148`, `9150`, `9151`, `9152`, `9153`, `9154`; đổi số `9143/9144/9145`.
- Backend: `Encounters/EncounterHandlers.cs` + `EncounterDto.cs`, `Reports/Engine/{SafeQueryBuilder,ReportBuilderHandlers,ReportDescriptor}.cs`, `Infrastructure/Reports/{GenericReportDataService,DatasetRegistry,ReportRegistry}.cs`, `Pharmacy/Dispensing`+`Dtqg`, `Bhyt/*`, `Patients/PatientQueryHandler.cs`, `LabResults`/`RadResults` handlers, `Api/Controllers/{Patients,LabResults,RadResults,Doctors}Controller.cs`, `Doctors/DoctorLookupHandlers.cs`, `Domain/Entities/AuditLog.cs`, `Infrastructure/Auth/PasswordHasher.cs`.
- Frontend: `components/layout/BranchSwitcher.tsx`, `lib/api/branches.ts`, `lib/hooks/use-branches.ts`, `lib/stores/branch-store.ts`, `lib/api/client.ts`, `app/(dashboard)/admin/branches/**`, `app/(dashboard)/encounters/_components/EncountersPageClient.tsx`, `components/domain/{DrugAutocomplete,ReceptionCheckInForm,VitalSignsForm,...}.tsx`, `lib/hooks/use-cls-rounds.ts`, `app/(dashboard)/nurse/_components/NursePageClient.tsx`.
- Docs: `TASKLIST-20260829.md`, `design/audit-token-mau-20260829.md`, `prd/dinh-nghia-nghiep-vu-da-chi-nhanh-20260829.md` (mục 15 — default 7 câu hỏi BO).
