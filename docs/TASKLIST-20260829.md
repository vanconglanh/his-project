# Task List tổng hợp — Pro-Diab HIS (2026-08-29)

> Gộp từ toàn bộ tài liệu điều tra trong phiên làm việc ngày 29/08/2026. Đây là nguồn tổng hợp
> để theo dõi — chi tiết từng mục xem link tài liệu gốc kèm theo. Trạng thái ✅ = đã fix & verify,
> 🔲 = chưa làm, ⚠️ = cần quyết định trước khi làm.

---

## A. Đã hoàn tất trong phiên này (không cần làm lại)

| # | Việc | Commit |
|---|---|---|
| ✅ | Merge `sys_phong_kham_noi` → `develop`, resolve 13 conflict | `173b514` |
| ✅ | Fix 3 Blocker + High vòng 1 QC (encounters/prescriptions/billings 500) | `8425357` |
| ✅ | Fix BUG-NEW-001 (cột `note`/`instructions`) + 6 chỗ `Guid.Empty` sai | `fb79944` |
| ✅ | Fix BUG-B3 (thêm dòng hoá đơn) + BUG-B2 (API guardian) + seed giá thuốc | `7e2b8f6` |
| ✅ | Fix rò rỉ dữ liệu chéo bệnh nhân (Allergies/Insurance/EmergencyContacts/Consents) + Package `sale_price` | `c489f6d` |
| ✅ | Hiển thị tên bệnh nhân màn Kết quả XN (danh sách + panel + chi tiết) | `aeb35ae` |
| ✅ | Panel đăng nhập nhanh theo role (dev-only) + seed 5 tài khoản test | `9c681d9`, `0c059e4` |
| ✅ | Bộ chọn "chỉ định XN đang chờ kết quả" cho form nhập KQ mới | `4952074` |
| ✅ | **RBAC: đồng bộ role↔permission** (167 mã đối chiếu, fix 403 hàng loạt 5 role) | `1d76873` |
| ✅ | 6 Blocker vòng full-flow: CCCD rỗng, ghi sinh hiệu, deadlock CLS, dropdown thuốc, URL kho, hoá đơn `params` async | `2114bcf`, `70640d3`, `33b1ac7` |
| ✅ | Fix CSS `prose`/placeholder/caret/outline cho trình soạn Bệnh án (4 vòng fix) | `7882535`, `94bf311`, `98318ea` |
| ✅ | Dropdown "Loại hồ sơ" cho upload tài liệu CLS | `85cea35` |
| ✅ | **RBAC Đợt 1**: cấp `cls_round.*` cho kế toán, `lab_result.verify`/`rad_result.verify` cho bác sĩ (đóng nửa P1-02, P1-03) | `9141_rbac_standard_alignment.sql` |
| ✅ | 9 Dialog bị kẹp 384px do bug Tailwind `sm:max-w-*` | `b4170bc` |
| ✅ | **Nhập liệu hàng loạt hồ sơ giấy cũ (Legacy Scan OCR Import)**: upload ZIP ảnh → OCR nền (Tesseract vie+eng, Hangfire queue `ocr`) → review/match bệnh nhân (auto theo `pat_patients.code` từ tên file / thủ công) → confirm → lưu đính kèm hồ sơ BN (`diab_his_fil_cls_uploads`+`fil_files`, KHÔNG tạo bệnh án). Migration `9187`, permission `legacy_import.write` (chỉ admin), 7 endpoint + màn admin + tab hồ sơ BN. Verify E2E: batch done 3/3, auto-match đúng BN thật, confirm ra tài liệu. Evidence: `docs/qc/evidence-legacy-ocr-import-20260830/`. Tiện tay vá bug Hangfire `Queues` (server chưa nghe queue non-default). | (commit phiên này) |
| ✅ | **Legacy Scan OCR Import — mở rộng định dạng ZIP**: thêm **PDF** (ưu tiên cao nhất, phổ biến nhất với máy scan văn phòng) — tầng 1 đọc text layer bằng `PdfPig` (đã dùng cho InBody OCR), tầng 2 fallback render từng trang bằng gói mới `PDFtoImage` (PDFium, hỗ trợ Win/Linux/macOS, không cần cài thêm gói hệ thống trên Docker) rồi OCR Tesseract, gộp text tất cả trang thành 1 item (1 PDF nhiều trang = 1 bộ hồ sơ 1 bệnh nhân). Thêm **TIFF/BMP** (dễ — Tesseract/Leptonica đọc sẵn). **HEIC/HEIF: KHÔNG hỗ trợ** — đánh giá rủi ro cao (native codec phức tạp, license/build trên Linux Docker rủi ro) so với lợi ích thấp (máy scan phòng khám hầu như không xuất HEIC) → chọn guard rõ ràng: tạo item `status=failed` kèm thông báo tiếng Việt yêu cầu đổi định dạng, KHÔNG âm thầm bỏ qua. Code: `IPdfTextExtractor`/`PdfTextExtractor`, `LegacyImportFileClassifier` (tách logic phân loại file để unit test độc lập), cập nhật `LegacyOcrBatchJob`. 918/918 unit test pass (+17 test mới, gồm 1 test verify thật bằng Tesseract engine thật OCR ảnh nhúng trong PDF giả lập scan). Evidence: `docs/qc/evidence-legacy-ocr-import-20260830/README.md`. | (commit phiên này) |

---

## B. 🔲 Bug còn tồn từ QC full-flow (chưa fix)

Nguồn: [`docs/qc/evidence-fullflow-20260829/README.md`](qc/evidence-fullflow-20260829/README.md)

| # | Bug | Mức | Mô tả ngắn | Vị trí |
|---|---|---|---|---|
| B-1 | BUG-09 | ✅ Done (2026-08-29) | Cột "Bệnh nhân" (và "Bác sĩ") trống trên `/billings`, `/cashier`, `/prescriptions` — thu ngân không biết đang thu của ai | Đã fix: `ListBillingsHandler` (`/billings`, `/cashier` dùng chung) + `ListPrescriptionsHandler` batch-load `patient_summary` theo trang, tránh N+1. FE đã sẵn code đọc `patient_summary` từ trước, chỉ backend thiếu join. |
| B-2 | BUG-10 | ✅ Done (2026-08-29) | Lỗi API hiện tiếng Anh thô ("Request failed with status code 403") thay vì message tiếng Việt backend đã trả sẵn | Đã fix: `PatientEditorLayout.tsx` dùng trực tiếp `err.message` thay vì gọi `getErrorMessage()` (helper đã có sẵn ở `lib/utils/errors.ts`, ưu tiên `error.response.data.error.message`). Các nơi khác đã dùng đúng helper hoặc là lỗi validation RHF (không liên quan). |
| B-3 | BUG-11 | ✅ Done (2026-08-29) | Root cause: `/nurse` queue lọc cứng `status:"WAITING"` → BN chuyển "Đang khám" (IN_PROGRESS) biến mất. Fix: `EncounterHandlers` hỗ trợ lọc CSV nhiều status; nurse queue truyền `"WAITING,IN_PROGRESS"`. |
| B-4 | UX #2 | ✅ Done (2026-08-29) | `use-cls-rounds.ts`: toast submit chỉ báo "chuyển thu ngân" khi `total_amount>0 && payment_status=UNPAID`, ngược lại chỉ "Đã chốt đợt chỉ định". |
| B-5 | UX #4,5 | ✅ Done (2026-08-29) | `DrugAutocomplete`/`ReceptionCheckInForm`: role combobox/listbox/option + aria; `VitalSignsForm`: htmlFor/id cho mọi input sinh hiệu. |
| B-6 | UX #7 | ✅ Done (2026-08-29) | `PasswordHasher` đọc `Security:BCryptWorkFactor` (dev=10); mig 9147 re-hash 6 tài khoản test về factor 10 (login dev nhanh). |

### B'. Dọn 6 lỗi High/Med sau retest vòng 2 (2026-08-31) — ✅ tất cả Done

Nguồn: [`docs/qc/ute-full-flow-20260831.md`](qc/ute-full-flow-20260831.md) §7.5 · Evidence: [`docs/qc/evidence-high-medium-fix-20260831/`](qc/evidence-high-medium-fix-20260831/). Gate: `dotnet build`/`dotnet test` **989 PASS/0 FAIL**, `tsc --noEmit` sạch.

| # | Case | Mức | Trạng thái | Nội dung fix |
|---|---|---|---|---|
| B'-1 | UTC-RX-05 (BUG-06) | High | ✅ Done (2026-08-31) | `DtqgHandlers.cs:141` `ExecuteScalarAsync<int?>`→`<string?>` (cột ID là GUID CHAR(36)) — hết 500. Verify API: 404 nghiệp vụ. |
| B'-2 | UTC-ENC-02 (BUG-05) | High | ✅ Done (2026-08-31) | `EncounterHandlers.cs` admit không fallback `doctor_id` về lễ tân; bác sĩ gán khi "Bắt đầu khám". Verify API+DB. |
| B'-3 | UTC-CLS-15 | High | ✅ Done (2026-08-31) | `LabResultOcrParser.cs` tách tiền tố mã XN (`GLU_F`→`GLU`) lấy alias. Unit test khoá. |
| B'-4 | UTC-DOC-04 (BUG-09') | Med | ✅ Done (2026-08-31) | `DocumentClassifierService.cs` thêm marker phiếu XN → nâng phiếu KQ XN thật lên `LabResult`. Unit test khoá. |
| B'-5 | UTC-EMR-08 (BUG-08) | Med | ✅ Done (2026-08-31) | mig `9190_emr_system_templates_structured_json.sql` seed `structured_json` cho 2 mẫu hệ thống. Verify DB. |
| B'-6 | UTC-RX-07 (BUG-10') | Med | ✅ Done (2026-08-31) | `PrescriptionHandlers.cs` `total_amount = SUM(line_total)` thay vì hardcode 0. Verify DB. |

---

### B''. Full-coverage fixes (2026-08-31) — ✅ tất cả Done

Nguồn: BO chốt 4 việc từ audit `docs/qc/itc-full-coverage-20260831.md`. Evidence: [`docs/qc/evidence-full-coverage-fixes-20260831/`](qc/evidence-full-coverage-fixes-20260831/).
Gate cuối: `dotnet test` **Unit 965 + Arch 7 + Integration 1193 = 2165 PASS / 0 FAIL / 0 SKIP**.

| # | Việc | Mức | Trạng thái | Nội dung |
|---|---|---|---|---|
| FC-1 | **Cross-tenant isolation** | 🔴 Bảo mật | ✅ Done | Bộ test mới `backend/tests/ProDiabHis.IntegrationTests/CrossTenant/` (seed 2 tenant, GET list không lẫn, GET /{id} record tenant B → 404). **PHÁT HIỆN + SỬA 4 lỗ hổng thật** thiếu `WHERE tenant_id` (Dapper): `BillingHandlers` (3) + `PrescriptionHandlers` (1) — có thể lộ PHI cross-tenant khi tham chiếu `patient_id` ngoài tenant. Commit `219daad`. |
| FC-2 | **BUG-001 FHIR AllowAnonymous** | High | ✅ Done | `RequirePermissionAttribute.OnAuthorization` nay kiểm `IAllowAnonymous` trong `EndpointMetadata` → cho qua. `GET /api/fhir/r4/metadata` trả 200 không cần token. Bỏ Skip test cũ → regression guard. Commit `73799d8`. |
| FC-3 | **Migration dựng DB sạch từ 0** | 🔴 Cao | ✅ Done | 30 file lỗi (Việc 3 agent) + **5 file lớp 91xx** leader re-verify độc lập phát hiện thêm (9135/9136/9138/9160 thiếu `DELIMITER`; 9171 quên drop `fk_lab_results_order`). Verify thật: fresh MySQL 8.0.36 volume, **211/211 file, 0 lỗi, 183 bảng** → không cần `EnsureCreated()`. Commit `3146342` + `d856a00`. |
| FC-4 | **Portal (PortalBearer) + B2B (ApiKey)** | Med | ✅ Done | Bộ test mới `Portal/` + `B2B/` (17 test): đăng nhập cổng BN, xem hồ sơ/KQ của chính mình, đặt lịch; isolation BN A/B; ApiKey thiếu/sai/sai-scope → 401/403. Commit `63b50b7`. |

---

## C. 🔲 Nợ kỹ thuật schema (đã biết, chưa fix — rủi ro cao nếu làm ẩu)

Nguồn: [`docs/leader/assignments-20260829-fixround.md`](leader/assignments-20260829-fixround.md) mục "Ưu tiên 3"

| # | Vấn đề | Ghi chú |
|---|---|---|
| C-1 | ✅ **HOÀN TẤT (2026-08-30)** — Bước 2 rewrite 30 tham chiếu (commit `c09b5e4`) + **Bước 3 DROP bảng chết** `diab_his_lab_orders` (mig `9171`). Verify DB thật trước drop: `lab_results LEFT JOIN cli_lab_orders` = **0 orphan**. Idempotent apply pass 2 sạch, build+747 test xanh sau drop. |
| C-2 | ✅ **HOÀN TẤT (2026-08-30)** — DROP `diab_his_rad_orders` (mig `9171`). Verify DB thật: `rad_results LEFT JOIN cli_rad_orders` = **0 orphan** + cột `cli_rad_orders` khớp đủ (modality/body_part/contrast/procedure_*/priority/status/ordered_*/note) + `rad_results` 0 dòng. FK `fk_rad_results_order` re-point sang `cli_rad_orders`. |

---

## D. 🔲 Design/UI debt (từ audit layout)

Nguồn: [`docs/design/layout-audit-20260829.md`](design/layout-audit-20260829.md)

| # | Việc | Mức | Ghi chú |
|---|---|---|---|
| D-1 | ✅ Done (2026-08-29) | Finding #2 | `ClsRoundCreateDialog`: grid `md:grid-cols-[1.2fr_1fr]`, `max-h-96`, `line-clamp-2` tên dịch vụ dài. |
| D-2 | ✅ Done (2026-08-29) | — | 13+ vị trí `text-[9/10/11px]` → `text-xs` (grep xác nhận 0 còn lại); +1 vị trí trong BranchSwitcher. |
| D-3 | ✅ **Audit Done (2026-08-29)** — [`docs/design/audit-token-mau-20260829.md`](design/audit-token-mau-20260829.md) | P2, quy mô lớn | Thực tế **77 file** (rộng hơn 42). Đã phân loại + xếp ưu tiên + khuyến nghị token semantic + `HisStatusBadge`. **Refactor** tách đợt riêng (không fix chung — đúng chủ trương), whitelist màn in ấn. |

---

## E. ⚠️ Đa chi nhánh — lộ trình triển khai (đã định nghĩa nghiệp vụ đầy đủ)

Nguồn: [`docs/prd/dinh-nghia-nghiep-vu-da-chi-nhanh-20260829.md`](prd/dinh-nghia-nghiep-vu-da-chi-nhanh-20260829.md) (115 business rule, đã có xác nhận BO cho Q3/Q4/Q8 + điều chuyển kho + giá override)

| Đợt | Nội dung | Phụ thuộc | Trạng thái |
|---|---|---|---|
| **Đợt 0** | Vá rò rỉ dữ liệu chéo chi nhánh | — | ✅ **Done (2026-08-29)** — đánh số lại migration trùng (cls/diagnosis → 9143/9144/9145, giữ họ branch 9080-9082); branch filter cho Dispensing/DTQG/BHYT + **phát hiện & vá report engine mù chi nhánh** (SafeQueryBuilder + DatasetRegistry 6 dataset + ~30+ report descriptor tĩnh); +8 unit test regression. Build+745 test xanh. P2-08 (ScopeMode) hoãn có chủ đích → cần architect. |
| **Đợt 1** | Branch switcher + màn quản lý chi nhánh (BR-37, BR-38); cột "Chi nhánh" lịch sử khám (BR-26); audit cross-branch (BR-24) | Đợt 0 | ✅ **Done (2026-08-29)** — BranchSwitcher (X-Branch-Id, đổi không cần login lại), màn `admin/branches` CRUD, cột "Chi nhánh" encounters (EncounterResponse.branch_id). Verify browser OK (đã fix crash MenuGroupContext). Cross-branch audit: nền tảng audit VIEW đã có (F/Đợt3 P0-01) + branch filter; audit *cross-branch attempt* riêng chưa tách. |
| **Đợt 2** | `branch_groups` (REGION) + `branch.group_view` (BR-02, BR-33); tìm kiếm BN theo phạm vi (BR-25); trạng thái chi nhánh (BR-08) | Đợt 1 | ✅ **Done (2026-08-29)** — mig `9150` (schema) + `9161` (permission). Handler: SearchPatientsQueryHandler guard cross-branch (BR-25/BR-33/BR-24), BranchScopeMiddleware resolve S2 group scope. commit `90ca998`. |
| **Đợt 3** | Điều chuyển kho nội bộ (state machine 8 trạng thái, ngưỡng 5tr); giá override 3 tầng (BR-70) + snapshot giá (BR-73) | Đợt 2 | ✅ **Done (2026-08-30) — có FE, xác nhận thật sự Done** — Backend: state machine điều chuyển kho 8+2 trạng thái (BR-54..62), controller `/api/v1/stock-transfers` (commit `90ca998`); giá override 3 tầng + PRICE_OVERLAP + snapshot billing_items (BR-70..76, commit `02c2156`). Build+747 test xanh. Frontend: trang danh sách `pharmacy/stock-transfers` (filter trạng thái/chi nhánh), tạo phiếu `pharmacy/stock-transfers/new` (autocomplete thuốc, cảnh báo ngưỡng 5tr BR-58), chi tiết `pharmacy/stock-transfers/[id]` (nút hành động đúng state machine + đúng quyền: submit/approve/reject/ship/receive/partial-receive/close/cancel), nav item "Điều chuyển kho". Verify qua API thật trên stack local: tạo phiếu → submit → self-approval bị chặn (`SELF_APPROVAL_NOT_ALLOWED`) → duyệt bởi user khác → ship chặn đúng khi thiếu tồn kho (`INSUFFICIENT_STOCK`) → receive chặn đúng khi sai trạng thái (`STOCK_TRANSFER_INVALID_STATE`). `npx tsc --noEmit` sạch. |
| **Đợt 4** | Dashboard chuỗi + xếp hạng + drill-down chi nhánh; công nợ nội bộ (BR-85, BR-87) | Đợt 3 | ✅ **Done (2026-08-30) — BE+FE, verify browser + E2E** — BE: `GET /dashboard/branch-ranking` (xếp hạng doanh thu/lượt/BN mới, % thay đổi kỳ trước, meta phạm vi BR-92/93) + `GET /dashboard/branch/{id}/detail` (drill-down bác sĩ BR-91), enforce scope S1/S2/S3 theo quyền `branch.cross_view`. Công nợ nội bộ: bảng `diab_his_bil_inter_branch_debts` (mig `9174`) + entity, sinh bút toán khi (a) BR-85 trả nợ chéo chi nhánh (debtor=CN thu tiền, creditor=CN phát sinh HD, doanh thu không đổi BR-86) và (b) BR-87 điều chuyển kho RECEIVED (đối soát nội bộ 1 pháp nhân, không hoá đơn theo Q3=Không). `GET /inter-branch-debts` (scope debtor OR creditor) + `POST /{id}/settle`. Report descriptor `inter-branch-debt` tự hiện trong `/reports`. FE: trang `/reports/chain-dashboard` (bảng xếp hạng + banner phạm vi + drill-down) + `/cashier/inter-branch-debts`. Commit `12ddee7` (BE), `764aaef` (FE). **Fix phát hiện qua verify**: BR-85 lookup HD bị branch query filter → `IgnoreQueryFilters` (commit `193936c`); scope màn READ cross-branch (commit `4bf31af`). Build 0 error, 879 test, verify browser + E2E (evidence `docs/qc/evidence-branch-dot4-5-20260830/`). |
| **Đợt 5** | ~~HĐĐT theo Facility~~ KHÔNG CẦN; BHYT theo chi nhánh (BR-107); clone chi nhánh + checklist go-live (BR-111/112); chuyển cơ sở nội bộ (BR-29) | Đợt 4 | ✅ **Done (2026-08-30) — BE+FE, verify browser + E2E** — BHYT/CSKCB per chi nhánh (mig `9175`: thêm field hospital_rank/kcb_tuyen/bhyt_contract_*/bhyt_enabled/dtqg_enabled + wire cột status BR-08). `GET /branches/bhyt-compliance` (BR-107). Guard BR-108 chặn khi thiếu cskcb tại **export XML 4210 + submit ĐTQG** (reception intake để TODO — xem dưới). Clone + go-live: chi nhánh mới mặc định DRAFT (BR-110), `POST /branches/{id}/clone` (copy phòng/kho/bộ đếm/giá override, KHÔNG copy BN/tồn kho/cskcb/credential/nhân sự — BR-111), `GET /branches/{id}/readiness` (checklist 8 mục BR-112) + `POST /branches/{id}/activate` (chặn nếu chưa đạt + audit). Chuyển cơ sở nội bộ (mig `9176`: bảng `diab_his_clinic_internal_referrals` + entity): `POST /internal-referrals`, `GET /incoming`, `PATCH /{id}/status` (BR-29). FE: mở rộng `BranchForm` (section BHYT/CSKCB), tab tuân thủ trong `/bhyt`, badge trạng thái + clone dialog + checklist dialog trong `/admin/branches`, trang `/encounters/referrals`. Commit `1057b39` (BE), `13a0605` (FE). **Fix phát hiện qua verify**: clone trùng unique code phòng/kho → suffix (commit `57d1701`). Build 0 error, 879 test, verify browser + E2E. |

**⚠️ Còn 7 câu hỏi BO chưa xác nhận** (đã có phương án đề xuất mặc định, dev có thể bắt đầu theo đề xuất): Q1 (kịch bản A+B), Q2 (giới hạn ≤50 chi nhánh/tenant), Q5 (ngưỡng duyệt kho 5tr), Q6 (trả nợ chéo chi nhánh), Q7 (bác sĩ xem bệnh án chi nhánh khác không giới hạn), Q9 (vai trò giám đốc khu vực), Q10 (đổi chi nhánh không cần đăng nhập lại). Chi tiết mục 10 tài liệu BRD.

---

## F. ⚠️ Phân quyền (RBAC) — đối chiếu chuẩn, có đề xuất cần quyết định

Nguồn: [`docs/prd/rbac-doi-chieu-chuan-20260829.md`](prd/rbac-doi-chieu-chuan-20260829.md)

**Kết luận:** Migration `9139` đúng chức năng nhưng chưa đủ kiểm soát theo Thông tư 13/2025/TT-BYT + Luật BVDLCN 2025 — thiếu audit truy cập đọc, vi phạm phân tách nhiệm vụ (SoD) ở 3 điểm.

| Đợt | Nội dung | Loại | Trạng thái |
|---|---|---|---|
| **Đợt 1 — an toàn, làm ngay** | Cấp `cls_round.read/pay/waive` cho `ke_toan`; cấp `lab_result.verify` + `rad_result.verify` cho `bac_si`. **Chỉ THÊM quyền, không thu hồi — rủi ro regression = 0** | SQL | ✅ **Đã áp dụng** — verify JWT qua API thật (2.2KB/1.7KB, xa ngưỡng 4KB), đúng danh sách quyền |
| **Đợt 2 — thu hồi quyền** | Thu hồi `cls_round.pay/waive` khỏi `bac_si`; `stock.adjust` + `report.build` khỏi `duoc_si` | SQL | ✅ **Done (2026-08-29)** — mig `9146`, TỰ QUYẾT theo least-privilege/SoD (BO không liên hệ được). Verify LIVE: đúng 3 quyền đã gỡ. **⚠️ Giả định BO review**: bác sĩ KHÔNG thu tiền CLS tại chỗ; phòng khám có kiểm kê kho tách khỏi dược sĩ. Rollback có sẵn trong file 9146. |
| **Đợt 3 — code compliance** | P0-01 audit VIEW; P1-04 mask PII report; P2-06 tách `patient.clinical.write`; P2-07 endpoint `doctors/lookup` | Code | ✅ **Done (2026-08-29)** — P0-01: `AuditAction.View` + ghi audit 5 điểm đọc chi tiết (patient/encounter/lab-rad PDF/prescription). P1-04: report engine mask PII mặc định, chỉ lộ khi `AllowPiiPlaintext`+`report.pii_plaintext` (mig 9154). P1-03 bonus: chặn self-verify CLS + tắt AutoVerify. P2-06: mig 9153 (`patient.clinical.write` cho bac_si+ktv), allergies đổi quyền — verify LIVE OK. P2-07: `GET /doctors/lookup`. Build+745 test xanh. 🔲 Còn: DELETE `user.read` (B5) chờ FE chuyển sang doctors/lookup. |

File SQL đã áp dụng (Phần A) + đề xuất còn chờ (Phần B/C, đang comment): [`db/migrations/9141_rbac_standard_alignment.sql`](../db/migrations/9141_rbac_standard_alignment.sql).

---

## G. Thứ tự đề xuất xử lý tiếp theo (ưu tiên chung toàn bộ)

1. **F/Đợt 1** — cấp thêm quyền CLS/lab-verify (an toàn tuyệt đối, 5 phút)
2. **B-1, B-2** — 2 bug UX ảnh hưởng thao tác hàng ngày của thu ngân (thiếu tên BN, lỗi tiếng Anh)
3. **E/Đợt 0** — vá rò rỉ dữ liệu chéo chi nhánh (rủi ro bảo mật, nên làm trước khi mở rộng thêm tính năng chi nhánh)
4. **F/Đợt 3, P0-01** — audit log VIEW (có hạn chót pháp lý 31/12/2026, nên lên lịch sớm dù chưa gấp)
5. Phần còn lại (C, D, E/Đợt 1-5, F/Đợt 2) — theo độ ưu tiên nghiệp vụ, cần bạn xác nhận thứ tự.

---

## H. 🔲 Đối chiếu SRS-HIS-phong-kham-noi-noi-tiet_V2 — chức năng còn thiếu/thiếu một phần

Nguồn: `C:\Users\ADMIN\Downloads\SRS-HIS-phong-kham-noi-noi-tiet_V2.docx` (user cung cấp 2026-08-29). Đã đối chiếu toàn bộ FR-1xx→FR-12xx với code hiện tại (route FE + controller/handler BE). Bảng dưới đây **chỉ liệt kê phần thiếu hoặc thiếu một phần** — các FR đã đủ (FR-101→104, FR-111, FR-201/202, FR-301-305, FR-312, FR-401-404, FR-411/412, FR-501-504, FR-511/512, FR-601/602/611, FR-701-703, FR-801/802, FR-901-903, FR-1001-1003, FR-1201/1202/1204) không đưa vào đây, xem báo cáo đối chiếu đầy đủ nếu cần (chưa lưu file riêng — có thể tái tạo qua Explore agent).

| # | Mã FR | Mức | Trạng thái | Thiếu gì | URL/route liên quan (đã có, cần bổ sung logic) |
|---|---|---|---|---|---|
| H-1 | FR-112 | P1 | ✅ Done (2026-08-29) | Đã có: `INotificationSender` + `SmsSender` (eSMS) + `ZaloZnsSender` (Zalo ZNS) đọc credential **per-tenant/branch mã hoá** (config được, reset qua UI không cần deploy lại). CRUD API `/api/v1/notification-channels` (+ `POST /{id}/test`), permission `notification_channel.read/write` (cấp admin). Background job `AppointmentReminderNotifyJob` (Hangfire, ngưỡng giờ cấu hình `Notifications:AppointmentReminderHours`, chống gửi trùng bằng cột `reminder_sent_at`). Verify: build sạch + tsc sạch + browser thật (tạo config giả → Test kết nối hiện lỗi tiếng Việt từ API eSMS/Zalo thật). | Migration [`db/migrations/9160_notification_channels.sql`](../db/migrations/9160_notification_channels.sql); route mới FE [/admin/notification-channels](../frontend/app/(dashboard)/admin/notification-channels); evidence [`docs/qc/evidence-h1-notification-20260829/`](qc/evidence-h1-notification-20260829/README.md) |
| H-2 | FR-203 | P1 | ✅ Done (2026-08-29) | Migration `9161` thêm permission `patient.cross_branch_search` + `cross_branch_view` (cấp admin, ke_toan). SearchPatientsQueryHandler dùng để mở khoá xem cross-branch. | commit `90ca998` |
| H-3 | FR-204 | P1 | ✅ Done (2026-08-29) | Xác nhận report engine ĐÃ có branch filter (E/Đợt0 vá ~30 descriptor via SafeQueryBuilder/DatasetRegistry). 3 report package mới (H-15) cũng enforce `BranchSql.Condition`. Không report nào mù chi nhánh. | E/Đợt0 + commit `c09b5e4` |
| H-4 | FR-304 | P0 | ✅ Done (2026-08-29) — Verify | `DeletePatientCommandHandler` là **soft-delete THẬT** (set `DeletedAt`/`DeletedBy` + audit log `DELETE`), KHÔNG hard-delete → KHÔNG vi phạm pháp lý. Query filter loại bản ghi `DeletedAt != null`. An toàn, không cần sửa. | `PatientCommandHandler.cs:262` |
| H-5 | FR-311 | P1 | ✅ Done (2026-08-29) | `ImageAnnotationDialog` (canvas vẽ/mũi tên/chữ nhật/text) trên ảnh CLS đã upload, lưu lại qua API upload hiện có. | commit `2e20464` |
| H-6 | FR-711 | P2 | ✅ Done (2026-08-29) | Endpoint đồng bộ batch readings CGM từ thiết bị, upsert theo (device_link, measured_at). | commit `04a414e` |
| H-7 | FR-803 | P1 | ✅ Done (2026-08-29) | Bắt buộc `encounterId` loại telehealth khi tạo đơn từ context telehealth (`TELEHEALTH_ENCOUNTER_REQUIRED`). | commit `04a414e` |
| H-8 | FR-804 | P2 | ✅ Done (2026-08-29) | Bảng `diab_his_tel_allowed_icd10` (mig `9170`) + CRUD admin + check ICD ngoài danh mục khi kê đơn telehealth (cấu hình được, không hardcode). | commit `04a414e` |
| H-9 | FR-911 | P1 | ✅ Done (2026-08-30) — có FE, xác nhận thật sự Done | Backend: QR VietQR **động** theo số tiền phải thu (`POST /billings/{id}/qr-dynamic`), cấu hình tài khoản qua settings. Frontend: nút "Thanh toán QR" trong `billings/[id]` mở `DynamicQrPaymentDialog` (component mới, tách khỏi `QrPaymentModal` cũ vì flow `/payments/qr/generate` có polling status còn `qr-dynamic` KHÔNG có — dùng nút "Xác nhận đã thanh toán" thủ công cho thu ngân, ghi rõ comment code đây là giải pháp tạm chờ webhook ngân hàng thật). Verify qua API thật: finalize hoá đơn → gọi `qr-dynamic` trả PNG hợp lệ (magic bytes `\x89PNG`) đúng amount/transaction_ref → xác nhận thủ công qua `POST /payments` (method `QR_VIETQR`) trả `status: COMPLETED`. `npx tsc --noEmit` sạch. | commit `02c2156` (BE) |
| H-10 | FR-1011 | P1 | ✅ Done (2026-08-29) | Policy `Security:MandatoryMfaRoles` (mặc định `admin`); login trả flag `mfaSetupRequired` khi role bắt buộc mà chưa bật 2FA. **⚠️ BO review**: role "Quản lý chi nhánh" chưa tồn tại trong DB → tạm map về `admin`; và luồng login hiện CHƯA verify 2FA lúc đăng nhập (gap có sẵn ngoài phạm vi task). | commit `1472c52` |
| H-11 | FR-1203 | P1 | ✅ Done (2026-08-29) — Verify | Backend ĐÃ có: `AddSubscriptionPaymentHandler` ghi `diab_his_pkg_payment_records` (payment_kind DEPOSIT/...) + tạo `bil_payments` cho từng lần thu, cập nhật `bil_billing` paid/balance. Không xuất khống phần chưa thu. | có sẵn `PackageSubscriptionHandlers.cs` |
| H-12 | FR-1205 | P1 | ✅ Done (2026-08-29) | Hiển thị "Còn X/Y lượt + HSD" real-time tại màn tiếp đón (ReceptionCheckInForm) dùng API `package-summary`. | commit `2e20464` |
| H-13 | FR-1206 | P1 | ✅ Done (2026-08-29) | Backend `PackageAlertJob` ĐÃ có sẵn đủ 4 rule (sắp hết hạn/định mức thấp/công nợ quá hạn/auto-expire), đã đăng ký RecurringJob trong `Program.cs`. FE thêm badge cảnh báo trên chi tiết bệnh nhân. | có sẵn + commit `2e20464` |
| H-14 | FR-1211 | P2 | ✅ Done (2026-08-30) | **BO đã CHỐT chính sách**: cho phép **gia hạn thêm X ngày** dùng định mức cũ (không mua gói mới, không cộng dồn). Backend `ExtendSubscriptionHandler` (chỉ khi `status=expired` + còn `remaining_quantity>0`, số ngày từ setting `package_expiry_extension_days` default 0=tắt); endpoint `POST /package-subscriptions/{id}/extend` + permission `package_subscription.extend` (mig `9172`); FE nút "Gia hạn". | commit `ac97bf6` |
| H-15 | FR-1212 | P2 | ✅ Done (2026-08-29) | 3 report descriptor mới: `package-revenue`, `package-utilization`, `package-outstanding-debt` (đều có branch + tenant filter). | commit `c09b5e4` |

**Ghi chú:**
- H-14 (FR-1211) là mục **duy nhất** trong bảng này cần hỏi lại BO trước khi làm — SRS gốc nói rõ đây là quyết định kinh doanh, không phải kỹ thuật thuần.
- Các mục còn lại (H-1→H-13, H-15) có thể tự quyết định phương án hợp lý và triển khai theo nguyên tắc "đừng hỏi, dùng sao cho tiện" đã thống nhất trong phiên trước — ưu tiên theo nhóm P0 > P1 > P2, và trong nhóm P1 nên làm H-9/H-11/H-12/H-13 trước (đều thuộc module Gói dịch vụ, liên quan trực tiếp thu ngân hàng ngày) rồi tới H-2/H-3 (trùng lộ trình đa chi nhánh ở mục E).
- H-4 (FR-304) nên kiểm tra sớm dù không phải "thiếu tính năng mới" — vì nếu `DELETE /patients/{id}` hiện tại là hard-delete thật thì đây là **vi phạm pháp lý** (SRS yêu cầu không hard-delete bệnh án trong toàn bộ vòng đời sản phẩm).

---

## I. 🔲 PRD Quét QR CCCD (2026-08-30)

Nguồn: [`docs/prd/quet-qr-cccd-20260830.md`](prd/quet-qr-cccd-20260830.md).

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| I-1 | Backend: check trùng CCCD 3 case + cập nhật chọn lọc | ✅ Done (2026-08-30) | `GET /api/v1/patients/check-cccd-duplicate` (NONE/EXACT_MATCH/FIELD_MISMATCH + field diff), `PUT /api/v1/patients/{id}/apply-cccd-fields` (audit log riêng từng field, `source=CCCD_QR_SCAN`, atomic 1 SaveChangesAsync). `CccdQrParser` (Application layer) + unit test parser/duplicate-check (22/22 pass). Commit `8a8c306`. |
| I-2 | Frontend: `CccdQrScanner`, dialog so sánh, tự điền form `/patients/new` + `ReceptionCheckInForm` | ✅ Done (2026-08-30) | `lib/api/types.ts` bổ sung `guardians`/`confirm_create_despite_duplicate` + type check-cccd-duplicate/apply-cccd-fields (đã sửa `possible_duplicate`/`duplicate_candidates` snake_case sau khi verify thật — không phải camelCase như đoán ban đầu, do JsonNamingPolicy.SnakeCaseLower convert cả anonymous object). `lib/api/patients.ts` + `lib/hooks/use-patients.ts` thêm `checkCccdDuplicate`/`applyCccdFields` + xử lý nhánh nghi trùng trong `createPatient()`. Component mới: `components/domain/CccdQrScanner.tsx` (bắt Enter từ máy quét keyboard-wedge, có nút "Đọc mã" dự phòng), `components/domain/CccdMismatchDialog.tsx` (bảng so sánh 4 cột, mặc định không tích checkbox), `lib/utils/cccd-qr.ts` (parser client-side mirror `CccdQrParser.cs`). Gắn vào `PatientEditorLayout.tsx` (mode create) + `ReceptionCheckInForm.tsx` (kèm dialog Case 2/3 + prefill sang `/patients/new` qua sessionStorage khi Case 1). `components/ui/checkbox.tsx` đã có sẵn từ trước, không cần tạo mới. Verify: rebuild `prodiab-backend` local (image cũ thiếu 2 endpoint) + gọi API thật xác nhận đúng cả 3 case + apply-fields + nhánh possible_duplicate; render `/patients/new` và `/reception` qua curl kèm cookie auth thật → HTTP 200, không lỗi compile/runtime, UI "Quét CCCD" xuất hiện đúng. `npx tsc --noEmit` sạch, eslint 0 error trên file mới/sửa. Evidence: `docs/qc/evidence-cccd-qr-20260830/`. |

---

## J. 🔲 Đọc kết quả máy InBody (trích PDF) (2026-08-30)

Nguồn: [`docs/prd/inbody-ocr-20260830.md`](prd/inbody-ocr-20260830.md).

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| J-1 | Backend: đọc PDF InBody + confirm ghi hồ sơ | ✅ Done (2026-08-30) | Kiến trúc tách interface `IInBodyDataProvider` (Application, `backend/src/ProDiabHis.Application/InBody/IInBodyDataProvider.cs`) — MVP `InBodyPdfTextProvider` (Infrastructure, dùng `UglyToad.PdfPig` đọc TEXT LAYER, KHÔNG OCR ảnh — giới hạn ghi rõ trong code + PRD); sau này thêm `InBodyApiProvider` chỉ cần đăng ký DI khác, không đổi contract. `InBodyReportParser` (Application, thuần regex label-based, không phụ thuộc PdfPig) trích 9 chỉ số: Weight/SMM/Body Fat Mass/PBF/BMI/Visceral Fat Level/TBW/BMR/InBody Score. Migration `db/migrations/9173_inbody_reports.sql` (idempotent, `CREATE TABLE IF NOT EXISTS`): bảng `diab_his_cli_inbody_report` (1 lần upload/đọc, `extraction_status` pending/success/partial/failed) + bảng mới `diab_his_cli_indicator_reading` (generic — dự án **chưa có sẵn bảng `ClinicalIndicator`** như giả định ban đầu, đã grep xác nhận chỉ có bảng cố định schema `diab_his_cli_diabetes_assessments` cho HbA1c/glucose/eGFR/BP, không phù hợp InBody, nên tạo bảng generic mới `indicator_type`/`value`/`unit`/`source='inbody_ocr'`). Endpoint: `POST /api/v1/patients/{patientId}/inbody-reports` (multipart PDF, extract + lưu `pending`, KHÔNG tự commit vào hồ sơ), `GET /api/v1/patients/{patientId}/inbody-reports` (lịch sử), `POST /api/v1/inbody-reports/{id}/confirm` (nhận field đã xác nhận/sửa từ FE → ghi cân nặng vào `diab_his_enc_vital_signs` nếu có `encounter_id` + include, các chỉ số còn lại vào `diab_his_cli_indicator_reading`; BMI CHỈ hiển thị, không persist riêng vì `VitalSigns` không có cột BMI). Permission dùng lại `patient.clinical.write` (upload/confirm) + `patient.read` (list) — không tạo quyền mới. Audit log `CREATE` (upload) + `CONFIRM` (xác nhận) trên resource `InBodyReport`. Unit test `InBodyReportParserTests` (5/5 pass: đủ field/viết tắt+layout khác/thiếu field không throw/text rỗng/null). Verify thật trên stack local: rebuild `prodiab-dev-backend`, apply migration 9173 vào MySQL thật, sinh 2 file PDF mẫu bằng QuestPDF (đủ field + thiếu field) gọi API thật qua curl → extract đúng 9/9 field (PDF đủ) và đúng 4/9 + đánh dấu thiếu 5/9 (PDF thiếu, không lỗi 500); confirm với giá trị cân nặng SỬA tay (68.4→68.5) → verify DB thật `diab_his_enc_vital_signs.weight_kg=68.50`, 7 dòng `diab_his_cli_indicator_reading` đúng `source=inbody_ocr`, 2 dòng audit log CREATE+CONFIRM; multi-tenant 404 đúng khi patient không tồn tại. `dotnet build` sạch (0 error, warning còn lại đều pre-exist không liên quan), `dotnet test` 858/858 pass (giữ nguyên toàn bộ test cũ + 5 test mới), ArchitectureTests 6/6 pass. Evidence: `docs/qc/evidence-inbody-ocr-20260830/`. |
| J-2 | Frontend: màn upload + xác nhận + lịch sử InBody | ✅ Done (2026-08-30) | API client `frontend/lib/api/inbody-reports.ts` (snake_case đúng response thật của backend, verify qua đọc code Controller/DTO) + hook `frontend/lib/hooks/use-inbody-reports.ts` (invalidate cả `vital-signs` list/latest/history khi confirm thành công để chart xu hướng cập nhật ngay). Component `frontend/components/domain/InBodyImportPanel.tsx` — dùng chung cho cả 2 nơi: (1) upload PDF (dropzone, hiển thị tên/size, nút "Tải lên & đọc"), (2) bảng xác nhận 9 chỉ số (nhãn tiếng Việt theo PRD mục 5, input sửa tay mọi field kể cả field đã đọc đúng, checkbox "Dùng" mặc định theo `extracted`, tô cảnh báo amber cho field `extracted=false`, cảnh báo đỏ riêng khi toàn bộ field thất bại — nghi ngờ PDF dạng scan ảnh — và khi thiếu `encounter_id` mà vẫn muốn lưu cân nặng). Gắn vào `NursePageClient.tsx` và `EncounterDetailClient.tsx`: thêm Tabs "Nhập tay" / "Nhập từ máy InBody (PDF)" cạnh `VitalSignsForm` hiện có trong Sheet ghi sinh hiệu (không thay thế form nhập tay). Component `frontend/components/domain/InBodyHistoryList.tsx` — tab "Lịch sử InBody" mới trong `patients/[id]/page.tsx`, danh sách các lần đo (badge trạng thái, tóm tắt chỉ số, link xem file gốc signed URL) + nút "Nhập kết quả InBody" mở Dialog dùng lại `InBodyImportPanel` (không gắn `encounter_id` khi thao tác từ hồ sơ bệnh nhân — chỉ lưu được các chỉ số khác ngoài cân nặng, đúng theo giới hạn API `confirm` cần `encounter_id` cho `WEIGHT_KG.include=true`). `npx tsc --noEmit` sạch, eslint sạch (chỉ còn 1 warning pre-existing `'vi' is defined but never used` trong `NursePageClient.tsx`, không liên quan thay đổi InBody — verify bằng `git diff` không đụng dòng đó). Backend đã verify đầy đủ bằng 2 PDF mẫu thật `docs/qc/evidence-inbody-ocr-20260830/sample-inbody-{full,partial}.pdf` (xem J-1) — FE dùng lại đúng 2 file này để test luồng upload/confirm qua UI. |

---

## K. ✅ Hướng dẫn sử dụng tương tác ngay trên web (product tour)

Nguồn: yêu cầu trực tiếp 2026-08-30 — **không phải** tài liệu Markdown có ảnh chụp (đã có sẵn ở `docs/user-guide/`, dùng cho nội bộ/đào tạo). Đây là tính năng **trong sản phẩm**: người dùng bấm nút "Hướng dẫn" ngay trên giao diện thật → hệ thống tự khoanh vùng + hiện tooltip dẫn dắt từng bước qua các chức năng của trang/vai trò đang đứng, giống Notion/Intercom/driver.js.

| # | Việc | Trạng thái | Ghi chú |
|---|---|---|---|
| K-1 | Chọn thư viện product-tour client-side | ✅ Done (2026-08-30) | Chọn `driver.js` (nhẹ, không phụ thuộc React) — engine dùng chung `frontend/lib/tours/engine.ts` (`runTour`, `runOnboardingTour`, `countAvailableSteps`, quản lý localStorage `tour-seen:{route}:{userId}`). |
| K-2 | Thiết kế cấu trúc khai báo bước tour theo từng trang/role | ✅ Done (2026-08-30) | `frontend/lib/tours/types.ts` (`TourDefinition`/`TourStep`, filter theo `permission`), mỗi trang 1 file khai báo riêng (`reception.ts`, `encounter-detail.ts`, `labrad.ts`, `cashier.ts`, `billing-detail.ts`, `pharmacy.ts`, `admin-branches.ts`, `onboarding.ts`), registry trung tâm `frontend/lib/tours/index.ts` (`resolveTour` theo route regex, `TOUR_CATALOG` cho trang /help). |
| K-3 | Nút "Hướng dẫn" đặt ở topbar/mỗi trang, kích hoạt đúng bộ bước theo trang hiện tại + vai trò đang đăng nhập | ✅ Done (2026-08-30) | `frontend/components/domain/TourButton.tsx` gắn trong `AppTopbar.tsx` — ẩn bước không đủ quyền (`resolveSteps` lọc theo `permission`/element có mặt trên DOM), disable nút khi 0 bước khả dụng. |
| K-4 | Ưu tiên phủ trước các trang/luồng chính | ✅ Done (2026-08-30) | Đã phủ 7 trang: Tiếp đón (`/reception`), Khám bệnh chi tiết (`/encounters/[id]`), CLS (`/labrad`), Thu ngân (`/cashier`), Chi tiết hoá đơn (`/billings/[id]`), Kho dược (`/pharmacy`), Quản lý chi nhánh (`/admin/branches`). **Chưa phủ** (đợt sau nếu cần): `/patients`, `/prescriptions`, `/bhyt`, `/reports/*`, các trang `/admin/*` còn lại (tenants/users/roles/audit/dtqg/suppliers/einvoice...), `/nurse`, `/diabetes/risk-list`, `/recall`, `/services`, `/appointments`. |
| K-5 | Nhớ trạng thái "đã xem hướng dẫn" theo user | ✅ Done (2026-08-30) | localStorage `tour-seen:{route}:{userId}` (tour trang lẻ) — tách riêng khỏi `tour-onboarding-seen:{userId}` (tour làm quen hệ thống, xem K-6). Không tự bật lại trừ khi bấm nút "Hướng dẫn" hoặc vào từ trang /help. |
| K-6 | Tour "Làm quen hệ thống" — chạy đúng 1 lần khi user lần đầu đăng nhập, gồm các thành phần dùng chung mọi trang (sidebar, đổi chi nhánh, tìm kiếm toàn cục, thông báo, menu tài khoản) | ✅ Done (2026-08-30) | `frontend/lib/tours/onboarding.ts` + `frontend/components/domain/OnboardingTour.tsx` (mount trong `app/(dashboard)/layout.tsx`, chạy khi `!isOnboardingSeen(userId)`). Thêm `data-tour` cho sidebar-nav/branch-switcher/global-search/notifications/user-menu. Cơ chế 2 tầng độc lập: onboarding chạy trước (chặn tour trang lẻ qua sự kiện `prodiab:onboarding-tour-done`), xong mới tới tour trang lẻ tự chạy như bình thường — không lặp bước trùng (sidebar/chi nhánh/search không khai báo lại trong 7 tour trang lẻ). **Bug đã fix trong lúc verify**: driver.js không luôn gọi `onDestroyed` một cách tin cậy khi đóng bằng nút X ở lần `driver()` thứ 2 trở đi trong cùng phiên trang (quan sát thực nghiệm, không rõ nguyên nhân sâu trong thư viện) → engine gọi thẳng `markTourSeen`/`markOnboardingSeen` ngay tại `onCloseClick` thay vì chỉ dựa vào `onDestroyed`. |
| K-7 | Trang "Trung tâm trợ giúp" `/help` — liệt kê tour theo module, lọc theo quyền, bấm "Xem lại hướng dẫn" điều hướng + tự chạy tour | ✅ Done (2026-08-30) | `frontend/app/(dashboard)/help/page.tsx` — nhóm theo module (dùng `TOUR_CATALOG`), lọc bằng `usePermissions().hasAny(entry.permissions)`. Deep-link `?tour=1` (strip khỏi URL sau khi kích hoạt) để trang đích tự chạy tour ngay khi load — xử lý trong `TourButton.tsx`. Tour gắn route động (`encounter-detail`, `billing-detail`) hiển thị nút "Đi tới trang danh sách" (không tự chạy được vì cần 1 bản ghi cụ thể). Link vào `/help` đặt trong `UserMenu.tsx`. |

**Trạng thái**: Done — verify qua Playwright thật (`frontend/e2e/product-tour-evidence.spec.ts` + `frontend/e2e/tour-onboarding.spec.ts`), evidence ảnh tại `docs/qc/evidence-product-tour-20260830/` (16 ảnh). `npx tsc --noEmit` sạch, eslint sạch.

---

## L. 🔲 Master data thuốc + Tham chiếu diaB + EMR template hoá (2026-08-30)

Nguồn: [`docs/prd/kien-truc-master-data-package-emr-20260830.md`](prd/kien-truc-master-data-package-emr-20260830.md) (v1.2). Tổng kết: [`docs/leader/tong-ket-emr-master-data-20260830.md`](leader/tong-ket-emr-master-data-20260830.md).

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| L-1 | **Mục 3** — Master data thuốc chuẩn BYT (route + bhyt_code, fix import Excel) | ✅ Done (2026-08-30) | Migration `9180_drug_route_bhyt_code.sql` (đã áp DB thật; PRE-CHECK 30 dòng thuốc, tất cả `*_need_sync=0` → SYNC 9010→9005 là no-op an toàn; `pha_drug_master` xác nhận là VIEW). `ClosedXmlImporter.cs` sửa ghi NHẦM bộ 9010 → nay ghi đúng bộ 9005 (`name/drug_form/sell_price/requires_rx/is_controlled`) + cột `route`, vẫn sync 9010 legacy cho báo cáo, INSERT vào bảng canonical `diab_his_pha_drugs`. `BhytXmlGeneratorImpl.cs`/`BhytXmlSql.cs` bỏ hardcode `"uong"`; lấy route theo `prescription_items.route → drugs.route`, cả 2 rỗng → KHÔNG phát hành XML, lỗi `DRUG_ROUTE_MISSING`. `Drug.cs`+`PharmacyConfiguration` map `Route`/`BhytCode`. Commit `a742a84`. |
| L-2 | **Mục 4** — Tham chiếu lộ trình diaB (phần HIS) + fix T1 walk-in | ✅ Done phần HIS / ⛔ Chặn phần diaB | `IExternalPathwayProvider` + `NullExternalPathwayProvider` (luôn `NotConfigured`, không gọi mạng, không lỗi). `GET /api/v1/patients/{id}/external-pathway` LUÔN trả HTTP 200 kèm status — verify thật: `200 / NOT_CONFIGURED / milestones=[]`. Fix T1 (§4.7.5): hook trừ định mức VISIT vào lúc bác sĩ MỞ khám (không phải check-in), `idempotency_key=ENCOUNTER:{id}:VISIT`, gộp luồng Appointment về cùng key → 2 luồng hội tụ 1 lần trừ; hết định mức không chặn khám; ghi `covered_by_subscription_id`. **CÒN CHẶN**: gọi API diaB thật — diaB CHƯA có endpoint (§4.7.1), đã code sẵn interface để cắm sau. Commit `eb55093`. |
| L-3 | **Mục 5** — EMR template hoá (form động + snapshot + hash v2) | ✅ Done (2026-08-30) | Migration `9181` (gộp diabetes_templates→EmrTemplate, `structured_json`, bảng nối `emr_template_package_map`, `covered_by_subscription_id`) + `9182` (`template_id`/`structured_values_json`/`schema_snapshot_json`). QĐ4: tách giá trị form (`structured_values_json` trên bệnh án) khỏi định nghĩa template. QĐ5: chụp `schema_snapshot_json` khi tạo version, render LUÔN theo snapshot. Hash chữ ký mở rộng v2 (`EmrSignPayload`) gộp 3 phần, giữ đường v1 cho bệnh án cũ. FE `DynamicFormRenderer` (8 loại field, group, colSpan) gắn màn khám + admin template. **Verify snapshot immutability qua API thật PASS**: sửa template gốc S1→S2, bệnh án đã ký VẪN render S1 (evidence `docs/qc/evidence-emr-template-master-data-20260830/`). `EmrSignFlowTests` 9/9 gồm case sửa structured_values/schema_snapshot sau ký → verify FAIL. Commit `04a78d9`. |

**Verify tổng**: `dotnet build` 0 error, `dotnet test` 895 pass (884 unit + 6 arch + 5 integration), `npx tsc --noEmit` sạch. Evidence: `docs/qc/evidence-emr-template-master-data-20260830/`.

---

## M. ✅ Ẩn/hiện + override giá theo chi nhánh cho THUỐC và DỊCH VỤ (2026-08-30)

Nguồn: BO chốt 2026-08-30. Mở rộng cơ chế override giá dịch vụ theo chi nhánh (E/Đợt3, bảng `diab_his_bil_service_branch_prices`, migration 9152) sang: (1) thêm cờ ẩn/hiện `is_active`; (2) áp dụng cùng cơ chế cho thuốc; (3) resolve logic dùng chung; (4) kê đơn/chỉ định tôn trọng cờ ẩn; (5) màn admin cấu hình.

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| M-1 | Migration `9185_branch_item_visibility_and_drug_prices.sql` | ✅ Done | Idempotent (`add_col_if_missing` + `CREATE IF NOT EXISTS` + `INSERT ... NOT EXISTS`). Thêm cột `is_active TINYINT(1) DEFAULT 1` vào `diab_his_bil_service_branch_prices`; tạo bảng mới `diab_his_pha_drug_branch_prices` (mirror bảng dịch vụ; `drug_id VARCHAR(36)` chứa được cả `ID` INT legacy lẫn CHAR(36) UUID vì `diab_his_pha_drugs` chạy runtime bằng Dapper, ID polymorphic); quyền `drug.price_override` cấp admin/quan_ly_vung. |
| M-2 | Backend resolve logic DÙNG CHUNG cho thuốc + dịch vụ | ✅ Done | `IBranchPriceResolver.ResolveAsync(tenantId, itemType: SERVICE\|DRUG, itemId, branchId, asOfDate)` trả `ResolvedItemPrice(Price, PriceSource, PriceOverrideId, IsActive)` — 3 tầng BRANCH→GROUP→TENANT (`BranchPriceResolverImpl`, Dapper, tham số hoá tên bảng/cột theo itemType). `IServicePriceResolver` cũ chuyển thành **adapter delegate** sang resolver chung (không còn 2 bản logic). CRUD override thuốc: `DrugPriceOverrideHandlers` + `DrugPriceOverridesController` (`/api/v1/drug-price-overrides`) mirror dịch vụ, có check `PRICE_OVERLAP`. Thêm `is_active` vào DTO/handler override giá dịch vụ. |
| M-3 | Kê đơn/chỉ định tôn trọng cờ ẩn (autocomplete) | ✅ Done | `BranchItemVisibilitySql` (helper dùng chung) sinh điều kiện SQL ẩn item theo đúng thứ tự ưu tiên resolver. Áp vào `SearchDrugsHandler` (thuốc — DrugAutocomplete) và `SearchServicesHandler` (dịch vụ — chỉ định CLS/thêm dòng hoá đơn): ẩn item bị tắt ở chi nhánh hiện tại (`IBranchProvider.BranchId`). Admin cross-view (`IgnoreBranchFilter`) hoặc không có branch context → thấy tất cả. |
| M-4 | Màn admin `admin/branch-pricing` (2 tab Dịch vụ/Thuốc) | ✅ Done | Bảng danh sách override + dialog thêm/sửa/xoá + toggle Hiện/Ẩn + ngày hiệu lực; hiển thị lỗi `PRICE_OVERLAP` rõ ràng. Chưa gắn menu sidebar (`AppSidebar` đang bị agent product-tour sửa song song) — truy cập trực tiếp route. |
| M-5 | Snapshot giá THUỐC vào dòng đơn/hoá đơn | 🔲 **Hoãn P2** | Dịch vụ đã có snapshot (`base_unit_price/price_source/price_override_id` ở `bil_billing_items`). Bảng `prescription_items` CHƯA có cột snapshot tương đương; theo chỉ đạo BO ưu tiên ẩn/hiện + override trước, snapshot giá thuốc trong đơn để đợt sau (chưa bắt buộc cho tính năng hiện tại). |

**Giả định đã dùng**: (a) THUỐC và DỊCH VỤ đều dùng `Guid Id`/`ID` map được về chuỗi → resolver chung tham số `string itemId`. (b) Nhóm chi nhánh (branch group) chưa có API riêng → form admin nhập `group_id` bằng số (Select khi có API `/branch-groups`). (c) Cờ ẩn/hiện dùng CHUNG cửa sổ hiệu lực với giá override (một dòng override vừa quyết định giá vừa quyết định ẩn/hiện theo `effective_from/to`).

**Verify tổng**: `dotnet build` 0 error; `dotnet test` **902 pass** (891 unit gồm +7 UTC drug override, 6 arch, 5 integration); `npx tsc --noEmit` sạch. Evidence browser: `docs/qc/evidence-branch-item-visibility-20260830/`. Commit BE `8218f64`, FE `df63123` (đã push develop).

## N. ✅ Fix 2 P0 blocker go-live (2FA thật + backfill id_number_bidx) (2026-08-30)

Nguồn: audit `docs/qc/go-live-readiness-20260830.md` mục 3 (#1, #2). Đây là 2 lỗi bảo mật/dữ liệu nghiêm trọng nhất phát hiện trong phiên.

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| N-1 | Thực thi 2FA thật ở backend | ✅ Done | Login bước 1 (user `TwoFaEnabled=true`) KHÔNG cấp AccessToken — chỉ cấp `mfaPendingToken` (aud=`mfa-pending`, TTL 5'). Endpoint mới `POST /api/v1/auth/2fa/verify` verify TOTP/recovery code + rate-limit (5 lần/5'), đúng mới cấp token đầy đủ. Role bắt buộc 2FA chưa bật → chặn, cấp `mfaSetupToken` (aud=`mfa-setup`) chỉ dùng cho `me/2fa/setup|enable`. Token tạm `aud` riêng → scheme Bearer mặc định từ chối API nghiệp vụ. |
| N-2 | Frontend luồng nhập TOTP khi login | ✅ Done | `LoginForm` 3 bước (credentials → mfa → mfaSetup inline); `verify2fa()` API; tái dùng `establishSession()`. tsc sạch. |
| N-3 | Fix cột `two_fa_recovery_codes` JSON→TEXT | ✅ Done | Migration `9186` (idempotent) + `UserConfiguration` bỏ `HasColumnType("json")`. Trước đó `me/2fa/enable` luôn 500 "Invalid JSON text" → 2FA không bật được. |
| N-4 | Backfill `id_number_bidx` (+ card_no, phone) | ✅ Done | Console command `dotnet run --project backend/src/ProDiabHis.Api -- backfill-bidx` (tái dùng `PiiBackfillService`, idempotent, mọi tenant). Fix bug decrypt marker-aware (`DecryptEnc`): `id_number_enc`/`card_no_enc` lưu RAW không marker, backfill cũ dùng `_pii.Unprotect` → bidx sai. Bổ sung backfill `phone_bidx` từ `phone_enc`. |
| N-5 | Cấu hình `Encryption:BlindIndexKey` runtime | ✅ Done (dev) / 📋 docs cho prod | Trước đó khoá không có trong runtime → dù backfill xong search vẫn hỏng. Đã set dev (appsettings.Development.json gitignored) + docs `docs/ops/backfill-id-number-bidx.md` yêu cầu set + BACKUP khoá mọi môi trường (mất khoá = mất search dữ liệu cũ vĩnh viễn). |

**Verify thật**: `dotnet build` 0 error; `dotnet test` **901 pass**; `npx tsc --noEmit` sạch. P0-1: API test 5 bước + browser end-to-end (bacsi.test bật 2FA → chặn/sai/đúng; letan.test không 2FA login thường). P0-2: DB `bidx_ok=enc_total=20`, blind index tự tính khớp DB, **`GET /patients/search?q=<CCCD cũ>` qua API trả đúng bệnh nhân**. Evidence: `docs/qc/evidence-p0-golive-fix-20260830/`.

**Còn tồn (không chặn dev/test, cần trước go-live prod)**: #3 Minio PublicEndpoint rỗng (P1); #4 checklist sinh secret khi deploy (P0 vận hành).

## O. ✅ OCR đọc kết quả xét nghiệm (CLS/Lab) khi nhập kết quả (2026-08-30)

Nguồn: gap thật — luồng nhập KQ XN (FR-503) cho upload file PDF/ảnh từ đối tác lab nhưng KHÔNG trích
giá trị, người dùng vẫn gõ tay. Tái dùng tối đa hạ tầng OCR đã có (InBody + Legacy-import), không viết lại engine.

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| O-1 | Provider điều phối OCR | ✅ Done | `LabOcrTextProvider` (Infrastructure/Lab) chỉ ĐIỀU PHỐI: PDF → `IPdfTextExtractor` (PdfPig + fallback OCR trang), ảnh → `IOcrTextProvider` (Tesseract vie+eng). KHÔNG viết lại engine. DI đăng ký ở block Lab. |
| O-2 | Parser theo XN đang chờ | ✅ Done | `LabResultOcrParser` (thuần, unit-test được). Với mỗi LabOrderItem ĐANG CHỜ của đúng encounter, dò số+đơn vị gần tên/mã XN (accent-insensitive, alias GLU/HBA1C/CHOL/TG/HDL/LDL/URE/CRE/AST/ALT/TSH...). Không đọc được → Extracted=false, không chặn field khác. |
| O-3 | Endpoint extract (không ghi DB) | ✅ Done | `POST /api/v1/lab-results/ocr-extract` [lab_result.write] — upload file + OCR + dò khớp XN chờ của encounter, trả giá trị để xác nhận. Stateless, không migration, không bảng mới. |
| O-4 | Endpoint confirm (tái dùng luồng có sẵn) | ✅ Done | `POST /api/v1/lab-results/ocr-confirm` [lab_result.write] — loop tạo LabResult qua `CreateLabResultCommand` (đã có payment gate G02, tính flag, SoD, audit). |
| O-5 | Frontend "Đọc từ file" | ✅ Done | `LabResultOcrPanel.tsx` (pattern InBody): upload → bảng review (Xét nghiệm \| Giá trị đọc được \| Đơn vị \| Chọn lưu, badge "Chưa đọc được") → xác nhận/sửa → lưu. Tab "Nhập tay"/"Đọc từ file" cạnh form cũ (giữ cả 2). Gắn ở `LabResultsTab` + Sheet trong encounter detail. |
| O-6 | Permission | ✅ Done | Dùng lại `lab_result.write` (không tạo quyền mới). |

**Verify thật**: `dotnet build` 0 error; `dotnet test` **927 pass** (+9 test OCR gồm đọc PDF thật bằng PdfPig); `npx tsc --noEmit` sạch. **E2E HTTP thật** (API local :5099 + MySQL dev): login KTV → upload PDF thật → extract đọc đúng GLU=7.2/HBA1C=8.10, GLU_F "chưa đọc được" → confirm (sửa tay GLU→7.5) → 2 `LabResult` tạo đúng trong DB (flag NORMAL, DRAFT, MANUAL). Evidence: `docs/qc/evidence-lab-result-ocr-20260830/`.

## P. ✅ Bộ phân loại tài liệu tự động + upload thông minh (smart-upload) (2026-08-30)

Nguồn: BO chốt — đã có 3 luồng OCR độc lập (InBody, Kết quả XN/CLS, Hồ sơ cũ), mỗi luồng nút/điểm vào
riêng. Thêm 1 lớp điều phối phía trước: upload 1 lần → tự OCR → tự nhận diện loại theo label đặc trưng →
tự route sang đúng luồng xác nhận có sẵn. KHÔNG viết lại 3 luồng, chỉ orchestrate.

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| P-1 | Bộ phân loại tài liệu | ✅ Done | `IDocumentClassifier`/`DocumentClassifierService` (Application, thuần, unit-test được). Chuẩn hoá text (lower, bỏ dấu). InBody: đếm label máy InBody (InBody Score/PBF/SMM/Visceral Fat...) ≥2 →0.9, 1 →0.6. LabResult: khớp tên/mã XN đang chờ của encounter (qua `IPendingLabTestsProvider`) ≥3 →0.9, 2 →0.75, 1 →0.55. Không khớp → Legacy 0.5 (fallback an toàn). Điểm < 0.6 → Unknown/để người dùng tự chọn. Trả evidence (label khớp) + candidates. |
| P-2 | Orchestrator + endpoint | ✅ Done | `SmartUploadCommandHandler`: OCR 1 lần (tái dùng `ILabOcrTextProvider`, không tạo engine mới) → classify → gọi lại `UploadInBodyReportCommand`/`ExtractLabResultOcrCommand`/`ExtractRadResultOcrCommand` qua IMediator. Legacy/Unknown/thấp → chỉ trả classification. `POST /api/v1/documents/smart-upload` [patient.clinical.write], giới hạn 20MB. Enum `DocumentType` serialize CHUỖI (JsonStringEnumConverter) cho FE ổn định. |
| P-3 | Provider pending lab | ✅ Done | `PendingLabTestsProvider` (Infrastructure, Dapper) query `diab_his_cli_lab_orders` pending theo encounter, có filter `tenant_id` + soft-delete + `NOT EXISTS` lab_results — cùng điều kiện luồng lab. |
| P-4 | Frontend lối tắt phụ | ✅ Done | Nút secondary "Tải tài liệu lên (tự nhận diện)" ở header `/patients/[id]` (KHÔNG phải CTA chính, theo khuyến nghị PO `docs/prd/vi-tri-nut-upload-ocr-20260830.md`). `SmartUploadDialog`: hiện loại + confidence + căn cứ, route sang khối xác nhận InBody/Lab (tái dùng mutation `useConfirmInBodyReport`/`useOcrConfirmLabResult`), hoặc dropdown 4 lựa chọn khi mơ hồ. **Giữ nguyên 3 entry point gốc.** |
| P-5 | Không migration | ✅ Done | Orchestration stateless, không thêm bảng. |
| P-7 | Mở rộng nhận **nhiều file cùng lúc** HOẶC **1 file ZIP** | ✅ Done (2026-08-31) | Endpoint `POST /api/v1/documents/smart-upload` đổi từ `IFormFile file` sang `IFormFileCollection files` (gom mọi file trong request — **tương thích ngược** cả client cũ gửi field `file`). Tách co chế giải nén ZIP an toàn (chống zip bomb/path traversal/giới hạn số file+dung lượng) từ `LegacyOcrBatchJob` ra helper dùng chung `SafeZipExtractor` (`backend/src/ProDiabHis.Application/Common/SafeZipExtractor.cs`) — refactor `LegacyOcrBatchJob` dùng lại helper này (KHÔNG viết lại). Handler batch mới `SmartUploadBatchCommandHandler`: nếu đúng 1 file `.zip` → giải nén; nếu nhiều file → xử lý trực tiếp; **mỗi file gọi lại `SmartUploadCommand` (xử lý-1-file) ĐỘC LẬP**, gom `SmartUploadItemResult` theo từng file (KHÔNG gộp). **Xử lý ĐỒNG BỘ** (trả ngay, không polling — dùng hàng ngày), cap 20 file/lần (>20 → `DOC_TOO_MANY_FILES`, hướng dẫn dùng Legacy-import cho batch lớn). 1 file OCR lỗi không làm hỏng file khác (item `success=false`). FE `SmartUploadDialog`: input `multiple` + nhận `.zip`, danh sách file chờ (thêm/xoá), sau xử lý hiện **danh sách thẻ kết quả riêng từng file** (`FileResultCard`) mở rộng xem chi tiết + **xác nhận riêng** (tái dùng nguyên `InBodySmartConfirm`/`LabResultSmartConfirm`/`RadResultSmartConfirm`/panel mơ hồ), lưu 1 file không đóng dialog (đánh dấu ✓ để xác nhận tiếp file khác). |
| P-6 | Bổ sung route CĐHA (RadResult) | ✅ Done (2026-08-31) | Thêm `DocumentType.RadResult` (giữ nguyên `JsonStringEnumConverter`). `ClassifyRadResultAsync` tái dùng `RadResultOcrParser.Parse` (marker "Mô tả"/"Nhận xét"/"Ket luận"/"Đề nghị" đã có sẵn từ tính năng OCR CĐHA) — Findings VÀ Conclusion cùng khớp →0.9; chỉ 1 trong 2 khớp là chưa đủ chắc chắn (dễ trùng chữ "Chẩn đoán" trong hồ sơ thường), chỉ tính điểm (0.55) khi có `EncounterId` VÀ đang có `RadOrder` chờ kết quả (`IPendingRadOrdersProvider` mới, Infrastructure/Dapper query `diab_his_cli_rad_orders` + `NOT EXISTS diab_his_rad_results`, cùng pattern `PendingLabTestsProvider`). `SmartUploadCommandHandler` route sang `ExtractRadResultOcrCommand` (stateless, không cần RadOrderId ở bước extract). FE: `SmartUploadDialog` thêm nhãn "Kết quả CĐHA", khối hiển thị Mô tả/Kết luận đọc trước + nút điều hướng sang tab "Kết quả CLS" để chọn RadOrder và xác nhận (khác InBody/Lab vì xác nhận CĐHA bắt buộc chọn `radOrderId` chưa có ở bước smart-upload). Đủ **5/5 loại tài liệu chính**: InBody / LabResult / RadResult / Legacy / Unknown. |

**Giả định đã dùng**: (a) "Không OCR lại 2 lần" áp cho classifier (tái dùng hạ tầng OCR có sẵn, không thêm engine mới); downstream InBody/Lab/Rad vẫn tự đọc lại file trong command gốc — KHÔNG refactor các luồng để tránh vi phạm "không viết lại". (b) Luồng Legacy là batch (ZIP) nên với 1 file lẻ, smart-upload không auto-ghi Legacy — chỉ đề xuất + điều hướng `/admin/legacy-import` (an toàn, đúng bản chất Legacy chỉ lưu đính kèm). (c) Case đã tự route (InBody/Lab) dùng khối xác nhận gọn tái dùng mutation gốc thay vì nhúng nguyên panel (tránh bắt tải lại file); riêng case RadResult chỉ hiển thị đọc trước + điều hướng (không tự confirm) vì luồng gốc bắt buộc chọn `radOrderId`.

**Verify thật**: `dotnet build` 0 error; test Documents **16/16 pass** (6 unit classifier gồm 2 case RadResult mới + 5 handler routing NSubstitute + 4 integration trên **PDF THẬT**: QuestPDF sinh PDF InBody/Hồ sơ cũ/KQ XN/KQ CĐHA → PdfPig đọc text → classifier). Case InBody → "InBody" 0.90; Case hồ sơ cũ ngẫu nhiên → "Legacy" 0.50 mặc định; Case KQ XN + pending → "LabResult" ≥0.6; Case KQ CĐHA (Mô tả + Kết luận thật) → "RadResult" 0.90. `dotnet test` toàn bộ **950 pass** (UnitTests) + ArchitectureTests + IntegrationTests đều xanh. `npx tsc --noEmit` sạch (exit 0). Evidence (kèm PDF thật, có case4 CĐHA): `docs/qc/evidence-smart-document-upload-20260830/`. Commit BE `bf60dfd`, FE `c83ac86`; bổ sung route CĐHA commit riêng ngày 2026-08-31.

## Q. ✅ OCR đọc kết quả chẩn đoán hình ảnh (CĐHA — X-quang/Siêu âm/CT) (2026-08-30)

Nguồn: CĐHA/`RadResults` chưa có OCR — bác sĩ/KTV gõ tay mô tả + kết luận từ phiếu đối tác gửi (PDF/ảnh).
Khác lab-result (trích **giá trị số** theo tên XN): CĐHA là **văn bản mô tả tự do** — 2 trường chính
"Mô tả"/"Findings" và "Kết luận"/"Impression". Tái dùng tối đa hạ tầng OCR đã có, không viết lại engine.

| # | Hạng mục | Trạng thái | Ghi chú |
|---|---|---|---|
| Q-1 | Provider OCR CĐHA | ✅ Done | `IRadOcrTextProvider`/`RadOcrTextProvider` — TÁI DÙNG `IPdfTextExtractor` (PdfPig + fallback Tesseract) và `IOcrTextProvider`, chỉ điều phối theo content-type. Không tạo engine đọc file mới. |
| Q-2 | Parser CĐHA | ✅ Done | `RadResultOcrParser` (thuần, unit-test được). Chiến lược **marker-based**: dò nhãn "Mô tả/Kết quả/Nhận xét/Hình ảnh"→findings, "Kết luận/Chẩn đoán"→conclusion, "Đề nghị/Khuyến nghị"→recommendations; nhãn KẾT THÚC (Bác sĩ thực hiện/Người đọc...) cắt phần chữ ký. Ràng buộc dấu `:` sau nhãn để loại va chạm tiêu đề ("PHIẾU KẾT QUẢ"). Chạy đúng cả khi PDF text-layer gộp cả trang thành 1 dòng. Giữ nguyên dấu tiếng Việt trong output. |
| Q-3 | Endpoints | ✅ Done | `POST /api/v1/rad-results/ocr-extract` (upload PDF/ảnh, OCR, KHÔNG ghi DB) + `POST /api/v1/rad-results/ocr-confirm` (TÁI DÙNG `CreateRadResultCommand` — giữ payment gate G02 + audit). Cả 2 dùng permission `rad_result.write` (không tạo quyền mới). |
| Q-4 | Frontend tab "Đọc từ file" | ✅ Done | `RadResultOcrPanel` đặt trong drawer "Nhập kết quả CĐHA" (`RadResultsTab`) dưới dạng Tabs cạnh form nhập tay — **không thay thế**, đúng pattern InBody/lab-result. Upload → hiện 2 ô lớn Mô tả/Kết luận điền sẵn → sửa tay → lưu. |
| Q-5 | Không migration | ✅ Done | Tái dùng bảng `diab_his_rad_results` + luồng create sẵn có, không thêm bảng/cột. |

**Giả định đã dùng**: (a) Nhãn phiếu CĐHA có dấu `:` sau tên mục (chuẩn phổ biến); nếu phiếu không có `:` mà nhãn nằm trọn 1 dòng riêng thì vẫn nhận. (b) "Nhận xét"→findings (theo yêu cầu), field `impression` để trống, người dùng tự điền ở màn xác nhận. (c) Confirm yêu cầu `rad_order_id`: khi mở từ nút chung, người dùng nhập/chọn mã chỉ định (không auto vì 1 màn có thể nhiều chỉ định).

**Verify thật**: `dotnet build` 0 error; `dotnet test` **946 pass** (+7 test OCR CĐHA gồm 1 test đọc **PDF thật** bằng PdfPig rồi parse). `npx tsc --noEmit` sạch (exit 0). **E2E HTTP thật** (API local :5099 + MySQL dev): login KTV (`ktv.test@prodiab.test`) → upload PDF phiếu X-quang thật → `ocr-extract` tách đúng Mô tả/Kết luận/Đề nghị, response **snake_case** (`has_any_extracted`, `raw_text` — đã verify đúng cảnh báo camelCase) → `ocr-confirm` (mô phỏng sửa tay) → `RadResult` tạo đúng trong `diab_his_rad_results` (giữ nguyên dấu tiếng Việt, status DRAFT, performed_by=KTV). Evidence: `docs/qc/evidence-radresult-ocr-20260830/`.

## R. ✅ Sửa gap chuỗi OCR theo PO review (2026-08-31)

Nguồn: [`docs/prd/po-review-ocr-flow-20260831.md`](prd/po-review-ocr-flow-20260831.md). BO chốt sửa GAP-8 (P0) + 6 gap P1 (GAP-1/2/3/6/7/9); phát hiện thêm 2 bug hệ thống khi sửa (Bug A, Bug B). GAP-4/5/10 (P2) để sau.

| ID | Nội dung | Trạng thái | Ghi chú |
|---|---|---|---|
| GAP-8 | Lab/Rad OCR lưu file gốc | ✅ Done | Upload file gốc lên MinIO (bucket `lab-ocr-sources`/`rad-ocr-sources`) + `fil_files` tại extract; lưu `source_file_id` vào `diab_his_lab_results`/`diab_his_rad_results` khi confirm; `source_file_url` (signed) + link "Xem file gốc" ở màn chi tiết. Migration `9188`. |
| GAP-2 | Lưu diff OCR gốc vs giá trị confirm | ✅ Done | Cột `ocr_raw_value` (Lab) / `ocr_raw_text` (Rad); FE gửi lại bản OCR gốc song song bản đã sửa. Migration `9188`. |
| GAP-3 | Cảnh báo giá trị ngoài khoảng vật lý (InBody + Lab) | ✅ Done | `InBodyPlausibleRanges`/`LabPlausibleRanges` → `out_of_plausible_range`/`plausible_range_note`; FE tô đỏ + checkbox bắt buộc trước khi xác nhận (không chặn cứng BE). |
| GAP-1 | InBody soft-delete báo cáo nhập nhầm | ✅ Done | `DELETE /api/v1/inbody-reports/{id}` (soft-delete `deleted_at`/`deleted_by`/`delete_reason`) + audit `DELETE` + nút "Huỷ" FE. Migration `9189`. |
| GAP-6 | Phân quyền InBody | ✅ Done (giữ nguyên) | Giữ `patient.clinical.write` — `le_tan` không được cấp quyền này → đã an toàn, không tách permission thừa. Căn cứ ghi trong po-review mục 4. |
| GAP-7 | Timeout + loading OCR | ✅ Done | Timeout 90s (`CancellationTokenSource.CancelAfter`) → `*_OCR_TIMEOUT` tiếng Việt; FE loading rõ + chặn double-submit. |
| GAP-9 | OCR đơn thuốc ngoài / giấy chuyển viện | ✅ Done | Mở rộng luồng legacy-import: `doc_type` whitelist (`HO_SO_CU_SCAN`/`DON_THUOC_NGOAI`/`GIAY_CHUYEN_VIEN`) + dropdown FE; không tự tạo đơn thuốc chính thức. |
| Bug A | FlagCalculator luôn nhận `null,null` → flag XN luôn NORMAL | ✅ Done | Join `diab_his_dict_lab_tests` theo `code` lấy `reference_range_low/high`, tính flag đúng + lưu range vào entity (Create + Import). |
| Bug B | BMI đọc được nhưng rơi mất khi confirm InBody | ✅ Done | Thêm `Bmi` vào `IndicatorTableTypes` → ghi vào `diab_his_cli_indicator_reading`. |

Commit: `9517d9a` (WP1 Lab/Rad + Bug A), `4cad41d` (test), `209c264` (WP2 InBody + Bug B), `fcd64d8` (GAP-9 legacy), + FE + evidence. Evidence: `docs/qc/evidence-ocr-gaps-fix-20260831/`.

---

## Sửa 4 Blocker go-live (2026-08-31, chiều)

Nguồn: [`docs/qc/go-live-readiness-nghiepvu-20260831.md`](qc/go-live-readiness-nghiepvu-20260831.md) + [`ute-full-flow-20260831.md`](qc/ute-full-flow-20260831.md). BO chốt sửa cả 4 Blocker ngay. Verify LIVE trên stack rebuild + DB thật. `dotnet test` **987 PASS / 0 FAIL** (+9 test mới), `tsc` sạch.

| ID | Nội dung | Trạng thái | Ghi chú |
|---|---|---|---|
| BUG-01 | Phát thuốc lỗi vẫn trừ kho thật | ✅ Done | Bọc `IDbTransaction` cho toàn bộ trừ kho + tạo phiếu; pre-check đủ tồn tất cả dòng trước khi ghi; FEFO insufficient → 422 thông báo rõ (không 500). Verify: tồn Metformin giữ nguyên khi phát lỗi. |
| BUG-02 | Phòng chỉ tiếp đón 1 BN/ngày | ✅ Done | Đếm sức chứa theo BN đang ở trong phòng (CALLED+IN_PROGRESS), không luỹ kế cả ngày. capacity=1 giữ nguyên. Verify: BN thứ 2 → 201. |
| BUG-03 | Ô chọn thuốc hiện sai/rỗng tên | ✅ Done | Thống nhất đọc/ghi cột `name` (nguồn thật 9005); migration `9191` đồng bộ `name`→`name_vi` + dọn junk TH001/TH002. Verify: search Metformin → đúng tên, 0 tên rỗng. |
| BUG-04 | Thu tiền/override giá âm/0/vượt | ✅ Done | Thêm 5 validator cấp Command bọc request (root cause: validator khai báo cho *Request không được pipeline gọi); chặn amount vượt số phải thu; test kiến trúc `ValidatorWrappingArchitectureTests` chặn tái phát. Verify: 0/-50000/999999999 đều 400. |

Commit: `da2f482` (BUG-01), `85878a4` (BUG-02), `4e98ebf` (BUG-03), `49452ed` (BUG-04). Evidence: `docs/qc/evidence-blocker-fix-20260831/`.

---

## Master data / Config qua UI admin (2026-09-01)

Nguồn: [`docs/prd/audit-hardcode-vs-master-data-20260901.md`](prd/audit-hardcode-vs-master-data-20260901.md). BO chốt: mọi config phải set được qua màn hình admin, ưu tiên kế thừa-override theo tenant. `dotnet test` **2165 PASS / 0 FAIL** (baseline giữ nguyên), `tsc` sạch, migration 9193/9194 idempotent verify thật trên DB. Evidence: `docs/qc/evidence-master-data-config-20260901/`.

| ID | Nội dung | Trạng thái | Ghi chú |
|---|---|---|---|
| N1 | `tenant_id` override cho code_master/detail + ICodeResolver + Admin CRUD API + màn `/admin/master-codes` | ✅ Done | Migration `9193` (tenant_id/tenant_scope/is_hidden/is_system, unique mới, perms code.read/code.manage). Resolver ưu tiên bản tenant > global, fallback mặc định, cache 5 phút. Verify DB: tenant A tự thêm/sửa/ẩn mã KHÔNG ảnh hưởng tenant B. |
| N2 | FE hardcode danh mục → `useCodes()` (giữ hằng số cũ làm fallback SSR/offline) | ✅ Done | patient-schema.ts (zod enum→string), PatientGeneralTab.tsx, code-labels.ts. ROLE giữ nguyên (có master table riêng). |
| N3 | Public settings endpoint + màn `/admin/settings` (mở rộng sys_settings) | ✅ Done | Migration `9194` (bảng `sys_setting_meta` nhãn tiếng Việt + is_public whitelist, perm setting.manage). `GET /settings/public`, `GET/PUT/DELETE /admin/settings`. |
| Bug-1 | Ngưỡng duyệt kho 5tr hardcode ở FE | ✅ Done | Xoá const, FE `useSettingNumber("stock_transfer_approval_threshold")` đọc từ `/settings/public`. |
| Bug-2 | SaveReportDialog hardcode 6 role | ✅ Done | FE `rolesApi.listRoles` (động), `ReportRoleCode=string`; BE validate `ROLE_NOT_FOUND` khi lưu report visibility=ROLE. |
| Bug-3 | LabPlausibleRanges giả định mmol/L | ✅ Done | `Check(...)` thêm tham số `unit`; detect mg/dL từ dữ liệu OCR → bảng ngưỡng riêng, không còn cảnh báo sai mọi kết quả mg/dL. |
| Bug phát sinh (verify) | Ẩn mã hệ thống theo tenant không hoạt động | ✅ Done | CodeResolver lọc `is_hidden=0` ở SQL làm lọt bản global. Fix: xử lý suppress ở tầng app. Re-verify DB PASS. |

---

*Tài liệu này là bản tổng hợp điều hướng — không thay thế các tài liệu gốc, chỉ trỏ tới chúng. Cập nhật lại khi có tiến triển mới.*

---

## Fix bug Dashboard + Gap Report P0 (2026-09-01)

Nguồn: [`docs/prd/review-dashboard-report-gap-20260901.md`](prd/review-dashboard-report-gap-20260901.md). PO Đăng review toàn bộ module Report/BI, fix 3 bug interface mismatch, gap analysis 9 report P0.

### Bug fixes (code sửa xong, chờ restart WSL dev server để apply)

| ID | Nội dung | Trạng thái | File sửa |
|---|---|---|---|
| BUG-D1 | Dashboard KPI cards hiện "—" — FE đọc nested field nhưng BE trả flat | ✅ Fixed | `lib/api/dashboard.ts`, `_components/DashboardOverview.tsx`, `vi.json` |
| BUG-D2 | Dashboard charts hiện "Chưa có dữ liệu" — FE đọc `points` nhưng BE trả `series` | ✅ Fixed | `lib/api/dashboard.ts`, `DashboardOverview.tsx` |
| BUG-D3 | FinancialTab "Xu hướng doanh thu" hiện "— đ" — FE đọc `total/by_breakdown` nhưng BE trả `total_revenue/series` | ✅ Fixed | `lib/api/reports.ts`, `FinancialTab.tsx` |

### Report P0 còn thiếu — cần làm trong sprint tiếp theo

| ID | Report | Nhóm | Ưu tiên sprint |
|---|---|---|---|
| C-03 | Recall list exportable (BN chưa tái khám đúng hạn — có thể gọi điện) | Lâm sàng | Sprint tiếp (L) |
| P-01 | Danh sách BN có gói dịch vụ sắp hết hạn < 30 ngày | Gói dịch vụ | Sprint tiếp (L) |
| C-05 | CLS bất thường flag ≠ NORMAL chưa được review | Lâm sàng | Sprint tiếp (M) |
| D-01 | Giá trị tồn kho theo nhóm (quantity × cost_price) — cần kế toán | Dược | Sprint tiếp (M) |
| F-01 | Tổng hợp doanh thu theo bác sĩ (xuất Excel + so KPI) | Tài chính | Sprint tiếp (M) |
| F-05 | Đối soát BHYT chi tiết (duyệt/từ chối/chờ theo kỳ) | Tài chính | Sprint sau (H) |
| A-02 | Dashboard trạng thái + export XML BHYT theo kỳ | Tuân thủ | Sprint sau (H) |
| F-02 | Đối soát ngân hàng/POS | Tài chính | ✅ Done (2026-09-01, import sao kê + auto-match) |
| C-01 | Trend HbA1c theo thời gian (nhiều tháng) | Lâm sàng ĐTĐ | Sprint sau (M) |

Chi tiết mục đích, role dùng, độ phức tạp: xem [`docs/prd/review-dashboard-report-gap-20260901.md`](prd/review-dashboard-report-gap-20260901.md).

---

## N. ✅ 9 báo cáo P0 Report/BI (2026-09-01) — HOÀN TẤT

Nguồn: [`docs/prd/review-dashboard-report-gap-20260901.md`](prd/review-dashboard-report-gap-20260901.md) §6. Toàn bộ 9 P0 triển khai bằng report engine (thêm ReportDescriptor tĩnh vào `ReportRegistry.cs` → tự có PDF/Excel + branch filter). Verify LIVE API + browser, seed tenant 1.

| ID | Code | Trạng thái | Rows LIVE |
|---|---|---|---|
| C-03 | `recall-due` | ✅ Done | 8 (4 quá hạn) |
| P-01 | `package-expiring` | ✅ Done | 3/6 (cửa sổ 30d) |
| C-05 | `cls-abnormal-unreviewed` | ✅ Done (chỉ XN, CĐHA hoãn) | 21 |
| D-01 | `ton-kho-theo-nhom` | ✅ Done | 14 nhóm |
| C-01 | `hba1c-trend` | ✅ Done | 6 tháng |
| F-05 | `bhyt-reconcile-detail` | ✅ Done | 11 items |
| A-02 | `bhyt-period-status` | ✅ Done | 3 kỳ |
| F-02 | `payment-method-reconcile` + import sao kê bank + auto-match | ✅ Done TRỌN VẸN (2026-09-01: thêm import Excel/CSV + auto-matching + màn `/cashier/bank-reconciliation`, mig 9195) | 14 |
| F-01 | `doanh-thu-theo-bac-si` | ✅ Done (KPI target hoãn) | 1 BS |

Bug fix kèm: `ReportExcelExporter.SanitizeSheetName` (Excel sheet name ≤31 ký tự). Gate: `dotnet test` 2165 pass/0 fail (giữ baseline), export PDF+Excel 200.

---

## O. ✅ 13 báo cáo P1/P2 Report/BI (2026-09-01) — HOÀN TẤT

Nguồn: [`docs/prd/review-dashboard-report-gap-20260901.md`](prd/review-dashboard-report-gap-20260901.md) §3+§5 (Backlog P1/P2). Toàn bộ triển khai bằng report engine (thêm ReportDescriptor tĩnh vào `ReportRegistry.cs` → tự có API data + PDF/Excel + branch/tenant filter, KHÔNG sửa FE). Verify LIVE qua API thật (container prodiab-backend build sẵn) + catalog FE. Seed tenant 1: `db/seeds/seed_reports_p1p2_tenant1.sql` (cleanup: `db/seeds/cleanup_reports_p1p2_tenant1.sql`).

| ID | Code | Nhóm | Rows LIVE | Ghi chú |
|---|---|---|---|---|
| F-03 | `refund-detail` | Tài chính | 3 (425.000đ) | Group theo lý do. Schema thiếu approver riêng → dùng paid_by |
| D-02 | `slow-moving-drugs` | Dược | 4 (1.259.200đ) | Tồn > N ngày (mặc định 90) không có EXPORT movement |
| A-01 | `audit-access-log` | Thống kê | 12 | `diab_his_sec_audit_logs`. Engine chưa gate per-report Admin-only (hạn chế đã ghi chú) |
| F-04 | `revenue-by-service` | Tài chính | 7 (720.000đ) | Mix dịch vụ + % đóng góp (window SUM OVER) |
| O-01 | `avg-wait-time` | Thống kê | 4 ngày | Từ `rcp_queue_tickets` (checked_in→called→finished) |
| O-02 | `revisit-rate` | Thống kê | 3 (30/60/90d) | Tỷ lệ tái khám 30d = 90% |
| D-03 | `drug-use-by-doctor` | Dược | 3 (285.000đ) | Chi phí thuốc theo BS |
| P-02 | `package-utilization-by-patient` | Tài chính | 6 | % dùng gói/BN, phát hiện churn (<30%) |
| C-02 | `diabetes-complications` | Lâm sàng | 5 loại | Parse `complications` JSON. Tổng hợp (patient_id INT không join UUID) |
| C-04 | `diabetes-risk-stratification` | Lâm sàng | 3 tầng | Cao/TB/Thấp theo HbA1c+biến chứng (assessment mới nhất/BN) |
| O-03 | `no-show-by-doctor` | Thống kê | 2 BS | No-show theo BS |
| O-04 | `appointment-performance` | Thống kê | 5 | Đúng giờ/trễ/hủy cuối phút (best-effort: dùng updated_at) |
| D-04 | `abc-inventory-analysis` | Dược | 2 | ABC (Pareto giá trị tiêu thụ) + XYZ (CV theo tháng) |

Gate: `dotnet build` sạch; `dotnet test` = **2165 pass / 0 fail / 0 skip** (giữ baseline); 13/13 `/data` API trả rows thật; export PDF (magic %PDF) + Excel (magic PK) 200; FE catalog render đủ 13 báo cáo đúng nhóm.

Giới hạn ghi rõ (không đoán, dựa schema/data thật):
- C-02/C-04: `diabetes_assessments.patient_id` là INT không join được `pat_patients` (UUID) → báo cáo tổng hợp, không liệt kê tên BN (theo tiền lệ `hba1c-trend`).
- A-01: report engine chỉ gate `report.read`/`report.export` chung, chưa có gate riêng Admin-only theo báo cáo.
- O-04: `sch_appointments` không có `checked_in_at` riêng → suy "đúng giờ/trễ/hủy cuối phút" từ `updated_at` (best-effort).
- D-04: XYZ cần lịch sử tiêu thụ nhiều kỳ; hiện tính trên `stock_movements` EXPORT (đã seed) — production cần dữ liệu xuất kho thực tế đủ dài để CV có ý nghĩa.
