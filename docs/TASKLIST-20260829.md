# Task List tổng hợp — Pro-Diab HIS (2026-08-29)

> Gộp từ toàn bộ tài liệu điều tra trong phiên làm việc ngày 29/08/2026. Đây là nguồn tổng hợp
> để theo dõi — chi tiết từng mục xem link tài liệu gốc kèm theo. Trạng thái ✅ = đã fix & verify,
> 🔲 = chưa làm, ⚠️ = cần quyết định trước khi làm.

---

## A. Đã hoàn tất trong phiên này (không cần làm lại)

| # | Việc | Commit |
|---|---|---|
| ✅ | Merge `sys_phong_kham_noi` → `develop`, resolve 13 conflict | `173b514` |
| ✅ | Fix 3 Blocker + High vòng 1 QC (encounters/prescriptions/billings 500) | `8425357` |
| ✅ | Fix BUG-NEW-001 (cột `note`/`instructions`) + 6 chỗ `Guid.Empty` sai | `fb79944` |
| ✅ | Fix BUG-B3 (thêm dòng hoá đơn) + BUG-B2 (API guardian) + seed giá thuốc | `7e2b8f6` |
| ✅ | Fix rò rỉ dữ liệu chéo bệnh nhân (Allergies/Insurance/EmergencyContacts/Consents) + Package `sale_price` | `c489f6d` |
| ✅ | Hiển thị tên bệnh nhân màn Kết quả XN (danh sách + panel + chi tiết) | `aeb35ae` |
| ✅ | Panel đăng nhập nhanh theo role (dev-only) + seed 5 tài khoản test | `9c681d9`, `0c059e4` |
| ✅ | Bộ chọn "chỉ định XN đang chờ kết quả" cho form nhập KQ mới | `4952074` |
| ✅ | **RBAC: đồng bộ role↔permission** (167 mã đối chiếu, fix 403 hàng loạt 5 role) | `1d76873` |
| ✅ | 6 Blocker vòng full-flow: CCCD rỗng, ghi sinh hiệu, deadlock CLS, dropdown thuốc, URL kho, hoá đơn `params` async | `2114bcf`, `70640d3`, `33b1ac7` |
| ✅ | Fix CSS `prose`/placeholder/caret/outline cho trình soạn Bệnh án (4 vòng fix) | `7882535`, `94bf311`, `98318ea` |
| ✅ | Dropdown "Loại hồ sơ" cho upload tài liệu CLS | `85cea35` |
| ✅ | **RBAC Đợt 1**: cấp `cls_round.*` cho kế toán, `lab_result.verify`/`rad_result.verify` cho bác sĩ (đóng nửa P1-02, P1-03) | `9141_rbac_standard_alignment.sql` |
| ✅ | 9 Dialog bị kẹp 384px do bug Tailwind `sm:max-w-*` | `b4170bc` |

---

## B. 🔲 Bug còn tồn từ QC full-flow (chưa fix)

Nguồn: [`docs/qc/evidence-fullflow-20260829/README.md`](qc/evidence-fullflow-20260829/README.md)

| # | Bug | Mức | Mô tả ngắn | Vị trí |
|---|---|---|---|---|
| B-1 | BUG-09 | 🟠 High | Cột "Bệnh nhân" (và "Bác sĩ") trống trên `/billings`, `/cashier`, `/prescriptions` — thu ngân không biết đang thu của ai | list component các màn này — áp dụng đúng pattern đã làm cho Kết quả XN (join tên BN vào response) |
| B-2 | BUG-10 | 🟡 Medium | Lỗi API hiện tiếng Anh thô ("Request failed with status code 403") thay vì message tiếng Việt backend đã trả sẵn | lớp hiển thị lỗi form nên đọc `error.response.data.error.message` trước khi fallback `error.message` |
| B-3 | BUG-11 | 🟠 High | Bệnh nhân "Đang khám" biến mất khỏi hàng chờ Điều dưỡng — **có thể đã bớt nghiêm trọng** sau khi nối form sinh hiệu vào màn bác sĩ (BUG-03), nhưng lỗi filter hàng chờ gốc **chưa xác nhận đã sửa** | `/nurse` queue filter |
| B-4 | UX #2 | Nhẹ | Toast "Đã chốt đợt chỉ định, chuyển thu ngân" hiện ra dù không có gì được chuyển thật — cần soát lại logic thông báo thành công theo đúng nghiệp vụ BUG-05 | luồng chốt đợt CLS |
| B-5 | UX #4,5 | Nhẹ (a11y) | Gợi ý bệnh nhân/thuốc không dùng `role="option"`/`listbox`; ô số sinh hiệu thiếu `label for` | combobox + VitalSignsForm |
| B-6 | UX #7 | Nhẹ | Đăng nhập mất ~6.9s (bcrypt work factor) — nên xem lại cho môi trường dev | `PasswordHasher.cs` |

---

## C. 🔲 Nợ kỹ thuật schema (đã biết, chưa fix — rủi ro cao nếu làm ẩu)

Nguồn: [`docs/leader/assignments-20260829-fixround.md`](leader/assignments-20260829-fixround.md) mục "Ưu tiên 3"

| # | Vấn đề | Ghi chú |
|---|---|---|
| C-1 | `diab_his_lab_results` (62 dòng): 60 dòng trỏ bảng chết `diab_his_lab_orders`, 2 dòng trỏ `cli_lab_orders` đúng | Swap mù mất 60/62 dòng — cần migration hợp nhất 2 họ bảng + sửa join đồng bộ (report/exporter). Đã có task chip theo dõi riêng. |
| C-2 | `diab_his_rad_results` FK trỏ bảng chết `diab_his_rad_orders`, nhưng rad order thật tạo ở `cli_rad_orders` | Latent bug, chưa lộ vì rad hiện 0 data thật. Sửa cùng đợt với C-1. |

---

## D. 🔲 Design/UI debt (từ audit layout)

Nguồn: [`docs/design/layout-audit-20260829.md`](design/layout-audit-20260829.md)

| # | Việc | Mức | Ghi chú |
|---|---|---|---|
| D-1 | Nội dung 2 cột trong `ClsRoundCreateDialog` vẫn chật dù dialog đã đúng kích thước (Finding #2) | P1 | Cần xem lại layout bên trong, không chỉ kích thước dialog |
| D-2 | 13 vị trí dùng `text-[9px]/[10px]/[11px]` dưới ngưỡng chuẩn (`text-xs`=12px là nhỏ nhất) | P2 (P1 riêng cho `DrugAutocomplete`/`DdiWarningPanel`/`CdssAlertBanner` vì ảnh hưởng đọc cảnh báo an toàn thuốc) | Đổi hết sang `text-xs` |
| D-3 | 42 file dùng Tailwind palette cứng (`bg-green-100`, `text-red-800`...) thay vì `HisStatusBadge`/token | P2, quy mô lớn | Tách thành audit token màu riêng, không làm chung đợt này |

---

## E. ⚠️ Đa chi nhánh — lộ trình triển khai (đã định nghĩa nghiệp vụ đầy đủ)

Nguồn: [`docs/prd/dinh-nghia-nghiep-vu-da-chi-nhanh-20260829.md`](prd/dinh-nghia-nghiep-vu-da-chi-nhanh-20260829.md) (115 business rule, đã có xác nhận BO cho Q3/Q4/Q8 + điều chuyển kho + giá override)

| Đợt | Nội dung | Phụ thuộc | Trạng thái |
|---|---|---|---|
| **Đợt 0** | Vá rò rỉ dữ liệu chéo chi nhánh: đánh số lại migration trùng (9080/9081/9082 trùng số — xem F báo cáo rapid assessment), lọc branch cho ~10 Dapper handler + report descriptor, bộ test tự động chặn regression | — | 🔲 Chưa làm — **nên làm sớm nhất, chặn mọi thứ sau** |
| **Đợt 1** | Branch switcher + màn quản lý chi nhánh (BR-37, BR-38); cột "Chi nhánh" trong lịch sử khám (BR-26); audit cross-branch (BR-24) | Đợt 0 | 🔲 |
| **Đợt 2** | `branch_groups` (chỉ type `REGION`, đã bỏ `HOSPITAL` theo Q3=Không) + `branch.group_view` (BR-02, BR-33); tìm kiếm bệnh nhân theo phạm vi, chặn tìm kiếm mở (BR-25); trạng thái chi nhánh (BR-08) | Đợt 1 | 🔲 |
| **Đợt 3** | Điều chuyển kho nội bộ (mục 4.2 BRD, state machine 8 trạng thái, ngưỡng duyệt 5tr) — **BO đã xác nhận cần làm**; giá override 3 tầng (BR-70) + snapshot giá vào `bil_billing_items` (BR-73) — **BO đã xác nhận** | Đợt 2 | 🔲 |
| **Đợt 4** | Dashboard chuỗi + xếp hạng + drill-down chi nhánh; công nợ nội bộ (BR-85, BR-87) | Đợt 3 | 🔲 |
| **Đợt 5** | ~~HĐĐT theo Facility (BR-80)~~ **KHÔNG CẦN LÀM** (Q3=Không, không có khách đa pháp nhân); màn tuân thủ BHYT theo chi nhánh (mỗi branch = 1 mã CSKCB, BR-107); clone chi nhánh + checklist go-live 8 mục (BR-111, BR-112); chuyển cơ sở nội bộ (BR-29) | Đợt 4 | 🔲 |

**⚠️ Còn 7 câu hỏi BO chưa xác nhận** (đã có phương án đề xuất mặc định, dev có thể bắt đầu theo đề xuất): Q1 (kịch bản A+B), Q2 (giới hạn ≤50 chi nhánh/tenant), Q5 (ngưỡng duyệt kho 5tr), Q6 (trả nợ chéo chi nhánh), Q7 (bác sĩ xem bệnh án chi nhánh khác không giới hạn), Q9 (vai trò giám đốc khu vực), Q10 (đổi chi nhánh không cần đăng nhập lại). Chi tiết mục 10 tài liệu BRD.

---

## F. ⚠️ Phân quyền (RBAC) — đối chiếu chuẩn, có đề xuất cần quyết định

Nguồn: [`docs/prd/rbac-doi-chieu-chuan-20260829.md`](prd/rbac-doi-chieu-chuan-20260829.md)

**Kết luận:** Migration `9139` đúng chức năng nhưng chưa đủ kiểm soát theo Thông tư 13/2025/TT-BYT + Luật BVDLCN 2025 — thiếu audit truy cập đọc, vi phạm phân tách nhiệm vụ (SoD) ở 3 điểm.

| Đợt | Nội dung | Loại | Trạng thái |
|---|---|---|---|
| **Đợt 1 — an toàn, làm ngay** | Cấp `cls_round.read/pay/waive` cho `ke_toan`; cấp `lab_result.verify` + `rad_result.verify` cho `bac_si`. **Chỉ THÊM quyền, không thu hồi — rủi ro regression = 0** | SQL | ✅ **Đã áp dụng** — verify JWT qua API thật (2.2KB/1.7KB, xa ngưỡng 4KB), đúng danh sách quyền |
| **Đợt 2 — cần hỏi trước khi làm** | Thu hồi `cls_round.pay/waive` khỏi `bac_si`; thu hồi `stock.adjust` + `report.build` khỏi `duoc_si` | SQL | ⚠️ **Cần hỏi: bác sĩ có thực tế thu tiền CLS tại chỗ không? Dược sĩ có phải người kiểm kê kho không?** |
| **Đợt 3 — backlog, cần architect thiết kế** | P0-01: audit log hành vi **VIEW** (đọc) hồ sơ bệnh án — **yêu cầu pháp lý bắt buộc, hạn chót 31/12/2026 theo TT 13/2025**; P1-04: report engine đang giải mã PII cho mọi role có quyền báo cáo (lỗ hổng vòng qua lớp mask); P2-06: tách `patient.clinical.write` khỏi `patient.write` (lễ tân hiện sửa được cả dị ứng thuốc); P2-07: endpoint `doctors/lookup` hẹp để bỏ `user.read` khỏi lễ tân/bác sĩ | Code | 🔲 Chưa làm — **P0-01 ưu tiên cao nhất, có hạn chót pháp lý** |

File SQL đã áp dụng (Phần A) + đề xuất còn chờ (Phần B/C, đang comment): [`db/migrations/9141_rbac_standard_alignment.sql`](../db/migrations/9141_rbac_standard_alignment.sql).

---

## G. Thứ tự đề xuất xử lý tiếp theo (ưu tiên chung toàn bộ)

1. **F/Đợt 1** — cấp thêm quyền CLS/lab-verify (an toàn tuyệt đối, 5 phút)
2. **B-1, B-2** — 2 bug UX ảnh hưởng thao tác hàng ngày của thu ngân (thiếu tên BN, lỗi tiếng Anh)
3. **E/Đợt 0** — vá rò rỉ dữ liệu chéo chi nhánh (rủi ro bảo mật, nên làm trước khi mở rộng thêm tính năng chi nhánh)
4. **F/Đợt 3, P0-01** — audit log VIEW (có hạn chót pháp lý 31/12/2026, nên lên lịch sớm dù chưa gấp)
5. Phần còn lại (C, D, E/Đợt 1-5, F/Đợt 2) — theo độ ưu tiên nghiệp vụ, cần bạn xác nhận thứ tự.

---

*Tài liệu này là bản tổng hợp điều hướng — không thay thế các tài liệu gốc, chỉ trỏ tới chúng. Cập nhật lại khi có tiến triển mới.*
